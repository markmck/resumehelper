---
phase: 20-skills-chip-grid
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/renderer/src/components/SkillChipGrid.tsx
  - src/renderer/src/components/ExperienceTab.tsx
autonomous: true
requirements: [VARNT-02, VARNT-03, VARNT-04]

must_haves:
  truths:
    - "Skills display as chips grouped by category in the Experience tab"
    - "A skill chip can be dragged from one category and dropped into another"
    - "A category name can be renamed inline by clicking it or the Rename button"
    - "A new empty category can be created via the Add category button"
    - "Dropping a skill on the new-category drop zone creates a category"
    - "Skills can be added inline via the + Add chip in each category"
    - "Skills can be deleted via the x button on each chip"
    - "Categories can be deleted with skills becoming uncategorized"
    - "Uncategorized skills appear in an Uncategorized group at the bottom"
  artifacts:
    - path: "src/renderer/src/components/SkillChipGrid.tsx"
      provides: "Full chip grid UI with DnD, inline editing, CRUD"
      min_lines: 200
    - path: "src/renderer/src/components/ExperienceTab.tsx"
      provides: "Swaps SkillList for SkillChipGrid"
      contains: "SkillChipGrid"
  key_links:
    - from: "src/renderer/src/components/SkillChipGrid.tsx"
      to: "window.api.skills"
      via: "IPC calls for skill CRUD and category CRUD"
      pattern: "window\\.api\\.skills"
    - from: "src/renderer/src/components/ExperienceTab.tsx"
      to: "src/renderer/src/components/SkillChipGrid.tsx"
      via: "import SkillChipGrid"
      pattern: "SkillChipGrid"
---

<objective>
Build the SkillChipGrid component that replaces SkillList in ExperienceTab. Implements the full chip grid UI with @dnd-kit cross-container drag, inline category rename, add/delete skills and categories, and the new-category drop zone.

Purpose: This is the primary user-facing deliverable — the chip grid that satisfies VARNT-02 (chip grid with DnD), VARNT-03 (inline rename), and VARNT-04 (add categories, drag into them).
Output: SkillChipGrid.tsx component, ExperienceTab updated to use it.
</objective>

<execution_context>
@D:/Projects/resumeHelper/.claude/get-shit-done/workflows/execute-plan.md
@D:/Projects/resumeHelper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-skills-chip-grid/20-CONTEXT.md
@.planning/phases/20-skills-chip-grid/20-RESEARCH.md
@.planning/phases/20-skills-chip-grid/20-UI-SPEC.md
@.planning/phases/20-skills-chip-grid/20-01-SUMMARY.md

<interfaces>
<!-- Types available after Plan 01 completes -->

From src/preload/index.d.ts (after Plan 01):
```typescript
export interface SkillCategory {
  id: number
  name: string
  sortOrder: number
}

export interface Skill {
  id: number
  name: string
  tags: string[]
  categoryId: number | null
  categoryName: string | null
}

// Api.skills:
skills: {
  list: () => Promise<Skill[]>
  create: (data: { name: string; tags: string[]; categoryId?: number | null }) => Promise<Skill>
  update: (id: number, data: { name?: string; tags?: string[]; categoryId?: number | null }) => Promise<Skill>
  delete: (id: number) => Promise<void>
  categories: {
    list: () => Promise<SkillCategory[]>
    create: (data: { name: string }) => Promise<SkillCategory>
    update: (id: number, data: { name?: string; sortOrder?: number }) => Promise<SkillCategory>
    delete: (id: number) => Promise<void>
  }
}
```

From src/renderer/src/components/BulletList.tsx (existing @dnd-kit sensor setup):
```typescript
import { DndContext, DragEndEvent, PointerSensor, KeyboardSensor, closestCenter, useSensor, useSensors } from '@dnd-kit/core'
import { sortableKeyboardCoordinates } from '@dnd-kit/sortable'

const sensors = useSensors(
  useSensor(PointerSensor),
  useSensor(KeyboardSensor, { coordinateGetter: sortableKeyboardCoordinates }),
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SkillChipGrid component with full DnD and interactions</name>
  <files>src/renderer/src/components/SkillChipGrid.tsx</files>
  <read_first>src/renderer/src/components/SkillList.tsx, src/renderer/src/components/BulletList.tsx, src/preload/index.d.ts, .planning/phases/20-skills-chip-grid/20-UI-SPEC.md</read_first>
  <action>
Create `src/renderer/src/components/SkillChipGrid.tsx` — a single-file component that replaces SkillList. Follow the UI-SPEC.md exactly for all styling, copy, and interactions. Use project tokens (var(--color-*), var(--space-*), var(--font-size-*), var(--radius-*)) not the mockup shorthand names.

**Component structure (all inline in one file — no separate CSS):**

1. **SkillChipGrid** (top-level):
   - State: `skills: Skill[]`, `categories: SkillCategory[]`, `loading: boolean`, `activeId: number | null` (for DragOverlay)
   - On mount: fetch `window.api.skills.list()` and `window.api.skills.categories.list()`
   - Group skills by categoryId. Categories with skills + Uncategorized group (skills with categoryId === null, only shown if any exist)
   - Wrap everything in `<DndContext>` with `PointerSensor` and `KeyboardSensor` sensors (same as BulletList.tsx pattern)
   - Use `closestCenter` collision detection
   - `onDragStart`: set `activeId` to the dragged skill id
   - `onDragEnd`:
     - If `over` is null, reset activeId and return
     - Parse target: if `over.id` starts with `"category-"`, extract category id via `parseInt(String(over.id).replace('category-', ''))`
     - If `over.id === "drop-zone-new-category"`, create a new category with name "New Category" via IPC, then move skill to it, and set the new category's name into edit mode
     - If target category differs from source: optimistic state update on `skills` array (set skill's categoryId), then call `window.api.skills.update(skillId, { categoryId: targetCategoryId })`
     - Reset activeId
   - `onDragCancel`: reset activeId
   - Render `<DragOverlay>` showing a clone of the active skill chip (full opacity, accent border, box-shadow)
   - Panel wrapper: `background: var(--color-bg-base)`, `border: 1px solid var(--color-border-subtle)`, `border-radius: var(--radius-xl)` (12px), `padding: var(--space-6)` (24px)

2. **Section header** (inside SkillChipGrid render):
   - Left: "Skills" title (18px, weight 500) + count badge (`N skills`, 12px, bg-raised, radius-sm, padding 2px 8px)
   - Right: "+ Add skill" button (btn-sm style per UI-SPEC). On click: find first category (or uncategorized) and activate its AddChip inline input.

3. **CategoryBlock** (inline sub-component or rendered section per category):
   - Register as droppable: `const { setNodeRef, isOver } = useDroppable({ id: \`category-\${category.id}\` })`
   - Category header: drag handle (visual only, `\u22EE\u22EE` text, text-muted, cursor: grab, non-functional per D-12), CategoryNameEditor, action buttons (Rename, Delete)
   - Chip grid area: `display: flex; flex-wrap: wrap; gap: 8px; min-height: 32px; padding: 4px; border-radius: var(--radius-md)`. When `isOver`: add drop-target styling: `background: rgba(139,92,246,0.06); border: 1px dashed rgba(139,92,246,0.3)`
   - Category block styles: bg-surface, border-subtle, radius-lg (8px), padding space-4, margin-bottom space-3

4. **CategoryNameEditor** (inline sub-component):
   - Props: `category: SkillCategory`, `onRename: (id: number, name: string) => void`, `editing: boolean`, `onStartEdit: () => void`, `onStopEdit: () => void`
   - Read mode: `<span>` with category name uppercase, 11px, weight 500, letter-spacing 0.05em, color text-tertiary. Click enters edit mode.
   - Edit mode: `<input>` same font/size/weight/transform, transparent bg, no border, accent bottom border when focused (`border-bottom: 1px solid var(--color-accent)`), width 180px, color text-primary. Commit on blur/Enter (call `window.api.skills.categories.update(id, { name: draft.trim() })`). Revert on Escape. Reject empty names.
   - The "Rename" button also triggers edit mode.

5. **SkillChip** (inline sub-component):
   - Register as draggable: `const { attributes, listeners, setNodeRef, isDragging } = useDraggable({ id: skill.id, data: { categoryId: skill.categoryId } })`
   - Styles per UI-SPEC: inline-flex, gap 4px, padding 4px 12px, radius-sm (4px), 13px, bg-raised, text-secondary, cursor grab, transparent border
   - Hover: border-emphasis, text-primary
   - Dragging (isDragging): opacity 0.5, border accent
   - Delete button (x): opacity 0.4 normal, 1.0 hover, 11px, cursor pointer. On click: `window.api.skills.delete(skill.id)` + optimistic removal from state
   - Apply `ref={setNodeRef}` and spread `{...listeners} {...attributes}` on the chip element

6. **AddChip** (inline sub-component per category):
   - Normal: dashed border-default, text-muted, 12px, "+ Add" text
   - Hover: border-emphasis, text-tertiary
   - On click: transform into inline text input (same chip dimensions, bg-input, solid accent border, text-primary, autoFocus)
   - On Enter: call `window.api.skills.create({ name: inputValue.trim(), tags: [], categoryId: category.id })`, add new skill to state, revert to "+ Add"
   - On Escape or blur with empty: revert to "+ Add"

7. **Uncategorized group** (per D-16):
   - Rendered at the bottom (before drop zone) only if skills with categoryId === null exist
   - Same CategoryBlock layout but: no Delete button, no drag handle, name = "UNCATEGORIZED"
   - Droppable id: `"category-uncategorized"` — in onDragEnd, parse this as categoryId = null
   - Has "+ Add" chip

8. **DropZoneNewCategory** (per D-09):
   - `useDroppable({ id: 'drop-zone-new-category' })`
   - Styles: dashed border rgba(139,92,246,0.2), bg rgba(139,92,246,0.03), radius-lg, padding space-4
   - Label: "DROP SKILL HERE TO CREATE NEW CATEGORY" — 11px, weight 500, uppercase, letter-spacing 0.05em, color accent-light (#a78bfa), opacity 0.6
   - Hidden drag handle (visibility: hidden) for spacing
   - Inner chip-grid area: min-height 40px. When isOver: rgba(139,92,246,0.06) bg + dashed rgba(139,92,246,0.3) border
   - On drop: create new category via IPC, move skill to it, new category appears with name in edit mode

9. **AddCategoryButton** (per D-08):
   - Full-width, dashed border-default, radius-lg, text-muted, 13px, padding 12px, cursor pointer
   - Hover: border-emphasis, text-tertiary
   - On click: `window.api.skills.categories.create({ name: 'New Category' })`, add to categories state, immediately enter edit mode on the new category's name

10. **Hint text** (per D-10):
    - "Drag skills between categories to reorganize." — 11px, text-muted, margin-top 8px, line-height 1.5

11. **Category delete flow** (per UI-SPEC Interaction States):
    - On Delete click: show inline confirmation replacing action buttons: "Delete -- this will move N skills to Uncategorized. Confirm?" with "Delete" and "Keep Category" buttons
    - Delete uses `--color-danger` (#ef4444) for the confirm button
    - Confirm: call `window.api.skills.categories.delete(catId)`. Optimistically move skills to uncategorized (set categoryId to null). Remove category from state.

12. **Empty state** (zero total skills):
    - "No skills added yet. Use \"+ Add skill\" to get started." — text-muted, font-size-sm, centered

13. **Loading state:** Simple "Loading skills..." message (matches existing SkillList pattern).

**Key implementation details:**
- All styles are inline React.CSSProperties objects (project pattern — no CSS modules)
- Use `useState` for hover states on buttons (project pattern from SkillList.tsx)
- Import from `@dnd-kit/core`: `DndContext`, `DragEndEvent`, `DragStartEvent`, `DragOverlay`, `PointerSensor`, `KeyboardSensor`, `closestCenter`, `useSensor`, `useSensors`, `useDroppable`, `useDraggable`
- Import `sortableKeyboardCoordinates` from `@dnd-kit/sortable` for KeyboardSensor config
- Prefix droppable ids with `"category-"` to avoid collision with skill integer ids (Pitfall 2)
- Use `DragOverlay` to render floating chip clone during drag (Pitfall 1 prevention)
- Track `editingCategoryId: number | null` state to control which category name is in edit mode
- Track `addingInCategoryId: number | null` state to control which category's AddChip is in input mode
  </action>
  <verify>
    <automated>cd D:/Projects/resumeHelper && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "SkillChipGrid" src/renderer/src/components/SkillChipGrid.tsx
    - grep -q "DndContext" src/renderer/src/components/SkillChipGrid.tsx
    - grep -q "useDroppable" src/renderer/src/components/SkillChipGrid.tsx
    - grep -q "useDraggable" src/renderer/src/components/SkillChipGrid.tsx
    - grep -q "DragOverlay" src/renderer/src/components/SkillChipGrid.tsx
    - grep -q "category-" src/renderer/src/components/SkillChipGrid.tsx
    - grep -q "drop-zone-new-category" src/renderer/src/components/SkillChipGrid.tsx
    - grep -q "window.api.skills.categories" src/renderer/src/components/SkillChipGrid.tsx
    - grep -q "UNCATEGORIZED" src/renderer/src/components/SkillChipGrid.tsx
    - grep -q "Drag skills between categories to reorganize" src/renderer/src/components/SkillChipGrid.tsx
  </acceptance_criteria>
  <done>SkillChipGrid component renders skills as chips grouped by category with full DnD between categories, inline category rename, add/delete skills and categories, new-category drop zone, and uncategorized group</done>
</task>

<task type="auto">
  <name>Task 2: Swap SkillList for SkillChipGrid in ExperienceTab</name>
  <files>src/renderer/src/components/ExperienceTab.tsx</files>
  <read_first>src/renderer/src/components/ExperienceTab.tsx</read_first>
  <action>
In `src/renderer/src/components/ExperienceTab.tsx`:

1. Replace import: change `import SkillList from './SkillList'` to `import SkillChipGrid from './SkillChipGrid'`
2. Replace usage: change `<SkillList key={refreshKey} />` to `<SkillChipGrid key={refreshKey} />`
3. Remove the `skillBadge` computation if it references SkillList-specific logic. The SkillChipGrid handles its own count badge internally. If `skillBadge` is used only as the `badge` prop on `<CollapsibleSection title="Skills" ... badge={skillBadge}>`, keep the CollapsibleSection badge but update the count source. Check if skillBadge fetches data separately — if so, it can stay since it's independent. If it was tied to SkillList state, remove it and let SkillChipGrid show the count internally.

The CollapsibleSection wrapper remains: `<CollapsibleSection title="Skills" defaultOpen={false}>` but remove the `badge` prop since SkillChipGrid has its own section header with count built in. Actually — check if CollapsibleSection renders its own title bar. If it does, the SkillChipGrid panel styling may conflict. In that case, either:
- (a) Remove the CollapsibleSection wrapper for Skills and let SkillChipGrid render as a standalone panel, OR
- (b) Keep CollapsibleSection but have SkillChipGrid NOT render its own panel wrapper (just the content)

Check how CollapsibleSection works and make the right choice. The SkillChipGrid has its own panel with border and padding per the mockup, so option (a) is cleanest — remove the CollapsibleSection wrapper for Skills and render `<SkillChipGrid key={refreshKey} />` directly.
  </action>
  <verify>
    <automated>cd D:/Projects/resumeHelper && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "SkillChipGrid" src/renderer/src/components/ExperienceTab.tsx
    - grep -qv "SkillList" src/renderer/src/components/ExperienceTab.tsx || echo "SkillList still referenced"
  </acceptance_criteria>
  <done>ExperienceTab renders SkillChipGrid instead of SkillList, no SkillList import remains</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- SkillChipGrid.tsx exists with DndContext, useDroppable, useDraggable, DragOverlay
- ExperienceTab imports and renders SkillChipGrid
- SkillList import removed from ExperienceTab
- All UI-SPEC styling values used (token names, not mockup shorthand)
- Droppable ids prefixed with "category-" (pitfall 2 prevention)
- DragOverlay renders floating chip clone (pitfall 1 prevention)
</verification>

<success_criteria>
- Skills display as chips grouped by category blocks
- Chips can be dragged between categories (DnD with @dnd-kit)
- Category names can be renamed inline
- New categories can be created via button or drop zone
- Skills can be added/deleted within categories
- Categories can be deleted (skills become uncategorized)
- All styling matches UI-SPEC token values
</success_criteria>

<output>
After completion, create `.planning/phases/20-skills-chip-grid/20-02-SUMMARY.md`
</output>
