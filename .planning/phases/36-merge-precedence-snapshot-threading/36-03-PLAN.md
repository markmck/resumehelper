---
phase: 36-merge-precedence-snapshot-threading
plan: 03
type: execute
wave: 1
depends_on: ["36-01"]
files_modified:
  - src/main/handlers/templates.ts
autonomous: true
requirements: [OVR-02]

must_haves:
  truths:
    - "setVariantOverride with non-empty text stores a variant-tier row (analysis_id NULL, source='user') verbatim"
    - "setVariantOverride with empty/whitespace text deletes the matching row"
    - "Calling setVariantOverride twice on the same entity replaces (no duplicate) via atomic delete+insert"
    - "clearVariantOverride deletes the matching variant-tier row"
    - "getVariantOverrides returns raw variant-tier rows only (analysis_id IS NULL), no base/effective enrichment"
  artifacts:
    - path: "src/main/handlers/templates.ts"
      provides: "getVariantOverrides / setVariantOverride / clearVariantOverride + IPC registration"
      exports: ["getVariantOverrides", "setVariantOverride", "clearVariantOverride"]
  key_links:
    - from: "registerTemplateHandlers"
      to: "ipcMain channels templates:getVariantOverrides/setVariantOverride/clearVariantOverride"
      via: "ipcMain.handle wiring (db-first)"
      pattern: "templates:setVariantOverride"
    - from: "setVariantOverride"
      to: "entity_overrides table"
      via: "sqlite.transaction delete+insert with isNull(analysisId) guard"
      pattern: "sqlite\\.transaction"
---

<objective>
Implement the three variant-tier override IPC handlers (read/write/clear) in templates.ts, following the variant-scoped getVariantOptions/setVariantOptions precedent and the acceptSuggestion atomic delete+insert pattern. These are the write/read API Phase 37's UI consumes; their merge-correctness is already covered because writes feed buildMergedBuilderData (Plan 02).

Purpose: Satisfies the handler half of OVR-02 / ROADMAP success criterion #3 (handlers registered + unit-tested round-trip). Implements D-02 (write/delete semantics) and D-03 (thin raw read).
Output: templates.ts handlers turn templates.variantOverride.test.ts GREEN.
</objective>

<execution_context>
@D:/Projects/resumeHelper/.claude/get-shit-done/workflows/execute-plan.md
@D:/Projects/resumeHelper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-merge-precedence-snapshot-threading/36-CONTEXT.md
@.planning/phases/36-merge-precedence-snapshot-threading/36-RESEARCH.md
@.planning/phases/36-merge-precedence-snapshot-threading/36-PATTERNS.md

<interfaces>
<!-- Extracted from templates.ts (VERIFIED) — executor uses directly. -->

templates.ts current head:
  line 2: `import { db } from '../db'`            → change to `import { db, sqlite } from '../db'`
  line 3: `import { templateVariants, templateVariantItems, jobBullets, projectBullets } from '../db/schema'` → add `, entityOverrides`
  line 4: `import { eq, and, desc, inArray } from 'drizzle-orm'` → add `isNull` (needed for variant-tier guard)
  type alias: `type Db = BetterSQLite3Database<typeof schema>`

registerTemplateHandlers() (lines 441-453): existing ipcMain.handle wiring ends at `templates:getThreshold` (line 453). Add three new handles after it.

Precedents:
  getVariantOptions/setVariantOptions — templates.ts:40-58 (db-first extracted fn shape)
  getOverrides raw-session prepare fallback — ai.ts:203-235 (session ? session.client.prepare : sqlite.prepare)
  acceptSuggestion atomic delete+insert — ai.ts:154-176 (sqlite.transaction(() => { delete; insert })())

Locked token table (verbatim):
  job bullet → 'job_bullet' / 'text' / bulletId
  summary    → 'summary'    / 'text' / (FK cols null)
  project    → 'project_name' / 'name' / projectId

Handler signatures (D-02/D-03):
  getVariantOverrides(db: Db, variantId: number) -> raw rows { entityType, field, bulletId, projectId, overrideText, source, createdAt } WHERE variant_id=? AND analysis_id IS NULL
  setVariantOverride(db, variantId, entityType, field, entityId: { bulletId?: number; projectId?: number }, text)
  clearVariantOverride(db, variantId, entityType, field, entityId: { bulletId?: number; projectId?: number })

Handler shapes: 36-RESEARCH.md Pattern 6 (lines 364-434); 36-PATTERNS.md templates.ts section (lines 238-340) + "Atomic Delete+Insert" (lines 590-624) + "Raw-Session Prepare Fallback" (lines 626-639).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement getVariantOverrides + clearVariantOverride; add imports</name>
  <files>src/main/handlers/templates.ts</files>
  <behavior>
    - getVariantOverrides(db, variantId) returns only rows with analysis_id IS NULL for that variant, raw shape, no enrichment.
    - getVariantOverrides does NOT return analysis-tier rows for the same variant.
    - clearVariantOverride deletes the matching variant-tier row (by variantId + analysis_id IS NULL + entityType + field + relevant FK) and returns success.
  </behavior>
  <read_first>
    - src/main/handlers/templates.ts (head lines 1-9, getVariantOptions 40-58, registerTemplateHandlers 441-453)
    - src/main/handlers/ai.ts getOverrides raw-session fallback (lines 203-235) and acceptSuggestion delete (lines 154-176)
    - 36-RESEARCH.md Pattern 6 getVariantOverrides + clearVariantOverride (lines 370-434)
    - 36-PATTERNS.md "Raw-Session Prepare Fallback" (lines 626-639) and templates.ts section (lines 238-340)
  </read_first>
  <action>
    Update imports: line 2 to `import { db, sqlite } from '../db'`; line 3 to add `entityOverrides`; line 4 to add `isNull` to the drizzle import. Implement exported `getVariantOverrides(db: Db, variantId: number)` mirroring ai.ts:203-235: resolve `const session = (db as any).session` with eslint-disable, build `prepare = session ? (sql) => session.client.prepare(sql) : (sql) => sqlite.prepare(sql)`, run a SELECT of entity_type AS entityType, field, bullet_id AS bulletId, project_id AS projectId, override_text AS overrideText, source, created_at AS createdAt FROM entity_overrides WHERE variant_id = ? AND analysis_id IS NULL, returning .all(variantId). No base/effective enrichment (D-03). Implement exported `clearVariantOverride(db, variantId, entityType, field, entityId)` using a drizzle delete on entityOverrides with where and(eq(variantId), isNull(entityOverrides.analysisId), eq(entityType), eq(field)) plus the relevant FK condition — when entityId.bulletId is provided add eq(entityOverrides.bulletId, entityId.bulletId), when entityId.projectId is provided add eq(entityOverrides.projectId, entityId.projectId); for summary (no FK) omit the FK condition. Return { success: true }. Validate entityType ∈ {'job_bullet','summary','project_name'} and throw/return an error result for unknown types (security — see threat model).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/handlers/templates.variantOverride.test.ts -t "getVariantOverrides"</automated>
  </verify>
  <acceptance_criteria>
    - templates.ts imports `sqlite`, `entityOverrides`, and `isNull`.
    - getVariantOverrides SQL contains `analysis_id IS NULL` and selects the raw columns (no JOIN to base text).
    - clearVariantOverride uses `isNull(entityOverrides.analysisId)` and the FK-conditional delete; returns { success: true }.
    - entityType validated against the allowed set before any write/delete.
    - The getVariantOverrides scope test (case 5: analysis-tier rows excluded) passes.
  </acceptance_criteria>
  <done>Read + clear handlers implemented with correct scope guard; getVariantOverrides scope test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement setVariantOverride (atomic upsert + empty-deletes) and register all three IPC handlers</name>
  <files>src/main/handlers/templates.ts</files>
  <behavior>
    - Empty/whitespace text → calls clearVariantOverride (delete), returns its result.
    - Non-empty text → atomic delete+insert: one variant-tier row (analysis_id NULL, source='user') with the trimmed-but-verbatim text; no compare-to-base.
    - Second call on same entity replaces the first (exactly one row remains).
  </behavior>
  <read_first>
    - src/main/handlers/ai.ts acceptSuggestion sqlite.transaction delete+insert (lines 154-176)
    - 36-RESEARCH.md Pattern 6 setVariantOverride (lines 395-419)
    - 36-PATTERNS.md "Atomic Delete+Insert" (lines 590-624) and registerTemplateHandlers extension (lines 325-337)
    - templates.ts registerTemplateHandlers (lines 441-453) for the wiring shape
  </read_first>
  <action>
    Implement exported `setVariantOverride(db, variantId, entityType, field, entityId, text)`. Validate entityType ∈ {'job_bullet','summary','project_name'}. Compute `const trimmed = text.trim()`; if `!trimmed` return clearVariantOverride(db, variantId, entityType, field, entityId). Otherwise run `sqlite.transaction(() => { ... })()` (mirrors ai.ts:154-176): first db.delete(entityOverrides).where(and(eq(variantId), isNull(entityOverrides.analysisId), eq(entityType), eq(field), <FK condition for bulletId or projectId, omitted for summary>)).run(); then db.insert(entityOverrides).values({ variantId, analysisId: null, entityType, field, bulletId: entityId.bulletId ?? null, projectId: entityId.projectId ?? null, overrideText: trimmed, source: 'user' }).run(). NOTE: D-02 says store verbatim (no compare-to-base) — store the text; trimming is used only to detect the empty-delete case (store `text` as-is or `trimmed` consistently per the test's expectation — match what templates.variantOverride.test.ts asserts; if the test seeds 'New text' the stored value must equal 'New text'). Return { success: true }. Do NOT use onConflictDoUpdate (Phase 35 D-01: partial unique index over nullable FKs unreliable). In registerTemplateHandlers (after line 453) add: `ipcMain.handle('templates:getVariantOverrides', (_, variantId) => getVariantOverrides(db, variantId))`; `ipcMain.handle('templates:setVariantOverride', (_, variantId, entityType, field, entityId, text) => setVariantOverride(db, variantId, entityType, field, entityId, text))`; `ipcMain.handle('templates:clearVariantOverride', (_, variantId, entityType, field, entityId) => clearVariantOverride(db, variantId, entityType, field, entityId))`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/handlers/templates.variantOverride.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - setVariantOverride uses `sqlite.transaction(...)()` for the non-empty path and returns clearVariantOverride for empty/whitespace text.
    - Insert sets analysisId: null and source: 'user'; no onConflictDoUpdate anywhere.
    - The stored overrideText matches the value the round-trip test asserts (verbatim, no compare-to-base deletion of identical text).
    - Three ipcMain.handle registrations added with db-first wiring.
    - `npx vitest run tests/unit/main/handlers/templates.variantOverride.test.ts` exits 0 (all five D-02/D-03 cases green: store, empty-deletes, replace-no-duplicate, clear, scope).
  </acceptance_criteria>
  <done>All variant-override handlers implemented, registered, and round-trip tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer (IPC) → main process handlers | variantId, entityType, field, entityId (FK ids), and text are renderer-supplied and untrusted; they cross into DB writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06 | Tampering | setVariantOverride/clearVariantOverride entityType from renderer | mitigate | Validate entityType ∈ {'job_bullet','summary','project_name'} before any write/delete; reject unknown values |
| T-36-07 | Tampering | SQL injection via renderer strings | mitigate | All writes use drizzle parameterized builders; getVariantOverrides raw SQL uses bound `?` params only (no string interpolation of variantId/text) |
| T-36-08 | Information disclosure | reading/clearing another variant's overrides | mitigate | All queries pin eq(entityOverrides.variantId, variantId) + isNull(analysisId); no cross-variant or cross-tier write path |
| T-36-09 | Tampering | invalid scope combo (analysis_id set on a 'user' variant write) | mitigate | setVariantOverride hardcodes analysisId: null + source: 'user'; renderer cannot supply analysisId |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: skipped) |

Note: input-length capping (e.g. 2000-char max per RESEARCH Security Domain) is low-risk for a local single-user Electron app; the bullet-edit UI already bounds text. Trim + verbatim store is sufficient for Phase 36; the write-path UI (Phase 37) owns any explicit length-limit affordance.
</threat_model>

<verification>
- `npx vitest run tests/unit/main/handlers/templates.variantOverride.test.ts` — all five round-trip cases green.
- `npx vitest run tests/unit/main/handlers/` — no regression in existing template/ai handler tests.
</verification>

<success_criteria>
- getVariantOverrides (thin raw read, analysis_id IS NULL only), setVariantOverride (empty-deletes, non-empty verbatim, atomic upsert, source='user'), clearVariantOverride implemented and exported.
- Three IPC channels registered in registerTemplateHandlers, db-first wiring.
- entityType validated against the allowed token set; no onConflictDoUpdate; isNull used for variant-tier guards.
</success_criteria>

<output>
Create `.planning/phases/36-merge-precedence-snapshot-threading/36-03-SUMMARY.md` when done.
</output>
