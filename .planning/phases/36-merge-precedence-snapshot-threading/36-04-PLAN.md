---
phase: 36-merge-precedence-snapshot-threading
plan: 04
type: execute
wave: 2
depends_on: ["36-02"]
files_modified:
  - src/main/handlers/submissions.ts
  - src/main/handlers/ai.ts
  - src/main/handlers/export.ts
autonomous: true
requirements: [OVR-03]

must_haves:
  truths:
    - "A submission snapshot freezes merged.summaryOverride into frozenProfile.summary (not profileRow.summary) when an override exists"
    - "Bullet/project-title overrides freeze into the snapshot for free via the merged builder arrays"
    - "runAnalysis builds scoring resumeText from an effectiveProfile whose summary is the variant summaryOverride when present (forward-compat threading)"
    - "The live DOCX export path honors summaryOverride"
  artifacts:
    - path: "src/main/handlers/submissions.ts"
      provides: "summaryOverride spliced into frozenProfile.summary"
      contains: "summaryOverride"
    - path: "src/main/handlers/ai.ts"
      provides: "effectiveProfile using summaryOverride for scoring text"
      contains: "effectiveProfile"
    - path: "src/main/handlers/export.ts"
      provides: "summaryOverride honored on live DOCX path"
      contains: "summaryOverride"
  key_links:
    - from: "src/main/handlers/submissions.ts buildSnapshotForVariant"
      to: "frozenProfile.summary"
      via: "summaryOverride ?? profileRow.summary ?? undefined"
      pattern: "summaryOverride \\?\\? profileRow\\.summary"
    - from: "src/main/handlers/ai.ts runAnalysis"
      to: "buildResumeJson(effectiveProfile, builderData)"
      via: "effectiveProfile summary override splice"
      pattern: "buildResumeJson\\(effectiveProfile"
---

<objective>
Thread merged.summaryOverride into the three callers that need it explicitly: the submission snapshot freeze (OVR-03 contract), the initial-run scoring text in runAnalysis (D-05, forward-compat), and the live DOCX export path. Bullet and project-title overrides already flow through the merged builder arrays, so these splices complete the override propagation to every surface.

Purpose: Satisfies OVR-03 (snapshots freeze the fully-merged text including variant-tier summary) and D-05 (initial-run scoring picks up the summary override). Closes the single-merge-path gaps where a consumer reads profileRow.summary directly.
Output: snapshot summary/bullet OVR-03 cases and the ai.runAnalysis D-05 case turn GREEN; live DOCX honors override.
</objective>

<execution_context>
@D:/Projects/resumeHelper/.claude/get-shit-done/workflows/execute-plan.md
@D:/Projects/resumeHelper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-merge-precedence-snapshot-threading/36-CONTEXT.md
@.planning/phases/36-merge-precedence-snapshot-threading/36-RESEARCH.md
@.planning/phases/36-merge-precedence-snapshot-threading/36-PATTERNS.md

<interfaces>
<!-- Extracted from source (VERIFIED) — executor uses directly. -->

MergedBuilderData now carries `summaryOverride?: string` (Plan 02). All three callers destructure `merged`.

submissions.ts buildSnapshotForVariant (lines 28-64):
  line 43: `const { showSummary, ...builderArrays } = merged`  → add summaryOverride: `const { showSummary, summaryOverride, ...builderArrays } = merged`
  lines 53-56 frozenProfile: line 55 `summary: profileRow.summary ?? undefined` → `summary: summaryOverride ?? profileRow.summary ?? undefined`
  `merged` already available (built at line 42 via buildMergedBuilderData(db, variantId, analysisId)); builderArrays spread (line 62) already freezes bullet/project overrides.

ai.ts runAnalysis scoring block (lines 83-87):
  current: `const merged = await buildMergedBuilderData(db, variantId)` / `const { showSummary: _showSummary, ...builderData } = merged` / `const profileRow = ...profile.id=1...get()` / `const resumeJson = buildResumeJson(profileRow, builderData)`
  target: destructure summaryOverride; `const effectiveProfile = profileRow && summaryOverride ? { ...profileRow, summary: summaryOverride } : profileRow`; `buildResumeJson(effectiveProfile, builderData)`
  Pitfall 4: buildResumeTextForLlm does NOT render basics.summary today — threading is forward-compat; do not expect scorer text to contain summary.

export.ts live DOCX path (lines 105-114):
  line 107: `const { showSummary, ...builderData } = merged` → add summaryOverride
  line 109: `buildResumeDocx(builderData, profileRow, layoutTemplate, templateOptions, showSummary)` → pass effectiveProfile (summaryOverride ?? profileRow.summary)
  Snapshot-render path (lines 147-227) reads frozenProfile from snapshot JSON — NO change (already frozen by submissions.ts).

Splice details: 36-RESEARCH.md Patterns 4 & 5 (lines 318-362); 36-PATTERNS.md submissions.ts (lines 173-202), ai.ts (lines 206-234), export.ts (lines 344-369).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Splice summaryOverride into snapshot freeze (OVR-03)</name>
  <files>src/main/handlers/submissions.ts</files>
  <behavior>
    - When a variant-tier summary override exists, snapshot.profile.summary equals the override text.
    - A variant-tier bullet override already appears frozen in the snapshot's builder arrays (no extra code — verify via test).
    - With no override, snapshot.profile.summary equals profileRow.summary (baseline unchanged).
  </behavior>
  <read_first>
    - src/main/handlers/submissions.ts buildSnapshotForVariant (lines 28-64, esp. destructure line 43 and frozenProfile lines 53-56)
    - 36-RESEARCH.md Pattern 4 (lines 318-332) and Pitfall 3 (lines 505-509)
    - 36-PATTERNS.md submissions.ts section (lines 173-202)
  </read_first>
  <action>
    Change the destructure at line 43 from `const { showSummary, ...builderArrays } = merged` to `const { showSummary, summaryOverride, ...builderArrays } = merged`. Change the frozenProfile summary property at line 55 from `summary: profileRow.summary ?? undefined` to `summary: summaryOverride ?? profileRow.summary ?? undefined`. No other change — the builderArrays spread already freezes bullet/project overrides. Run the snapshot test to confirm the OVR-03 summary + bullet cases pass and the baseline case still holds.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/handlers/submissions.snapshot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Destructure at line 43 includes `summaryOverride`.
    - frozenProfile summary reads `summaryOverride ?? profileRow.summary ?? undefined`.
    - `npx vitest run tests/unit/main/handlers/submissions.snapshot.test.ts` exits 0 — all three OVR-03 cases green (summary-override, bullet-override-frozen, baseline-no-override) plus pre-existing cases.
  </acceptance_criteria>
  <done>Snapshot freezes summary override; OVR-03 snapshot tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread summaryOverride into scoring (D-05) and live DOCX export</name>
  <files>src/main/handlers/ai.ts, src/main/handlers/export.ts</files>
  <behavior>
    - runAnalysis constructs effectiveProfile = { ...profileRow, summary: summaryOverride } when a variant summary override exists, then builds resumeJson from it.
    - runAnalysis still completes and returns an analysisId (no assertion that scorer text contains the summary — Pitfall 4).
    - Live DOCX export builds the doc from an effective profile honoring summaryOverride.
  </behavior>
  <read_first>
    - src/main/handlers/ai.ts runAnalysis scoring block (lines 83-87)
    - src/main/handlers/export.ts live DOCX path (lines 105-114) and snapshot-render path (lines 147-227 — confirm no change needed there)
    - 36-RESEARCH.md Pattern 5 (lines 334-362) and Pitfall 4 (lines 511-515)
    - 36-PATTERNS.md ai.ts section (lines 206-234) and export.ts section (lines 344-369)
  </read_first>
  <action>
    In ai.ts (lines 83-87): change the destructure to `const { showSummary: _showSummary, summaryOverride, ...builderData } = merged`; after fetching profileRow add `const effectiveProfile = profileRow && summaryOverride ? { ...profileRow, summary: summaryOverride } : profileRow`; change `buildResumeJson(profileRow, builderData)` to `buildResumeJson(effectiveProfile, builderData)`. Add the forward-compat comment noting buildResumeTextForLlm does not currently render basics.summary. In export.ts live DOCX path (lines 105-109): change the destructure at line 107 to include `summaryOverride`; add `const effectiveProfile = profileRow && summaryOverride ? { ...profileRow, summary: summaryOverride } : profileRow`; change the buildResumeDocx call (line 109) to pass `effectiveProfile` instead of `profileRow`. Leave the export.ts snapshot-render path (lines 147-227) untouched — frozenProfile is already frozen with the override by submissions.ts. Run the runAnalysis test (D-05 case) and the full handler suite.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/handlers/ai.runAnalysis.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - ai.ts contains `effectiveProfile` and calls `buildResumeJson(effectiveProfile, builderData)`; summaryOverride destructured from merged.
    - export.ts live DOCX path destructures summaryOverride and passes effectiveProfile to buildResumeDocx; snapshot-render path unchanged.
    - `npx vitest run tests/unit/main/handlers/ai.runAnalysis.test.ts` exits 0 (D-05 case green; runAnalysis returns analysisId; no assertion on summary in scorer text).
    - No regression in export tests.
  </acceptance_criteria>
  <done>Scoring + live DOCX honor summaryOverride; D-05 test green; snapshot-render path correctly left untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merged builder data → snapshot JSON / scorer text / DOCX | Already-merged override text (from trusted main-process merge) is frozen into a snapshot, sent to the LLM scorer, or rendered to DOCX. No new renderer input crosses here; the override values originated from validated handler writes (Plan 03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-10 | Tampering | snapshot freezes wrong (base) summary, losing OVR-03 guarantee | mitigate | summaryOverride ?? profileRow.summary splice; OVR-03 snapshot test asserts frozen value equals override |
| T-36-11 | Information disclosure | LLM scorer receives unintended profile data | accept | effectiveProfile only substitutes the summary field that the user themselves authored as an override; no new data leaves the trust boundary that wasn't already sent. Forward-compat (buildResumeTextForLlm omits summary today). |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: skipped) |
</threat_model>

<verification>
- `npx vitest run tests/unit/main/handlers/submissions.snapshot.test.ts tests/unit/main/handlers/ai.runAnalysis.test.ts` — OVR-03 + D-05 cases green.
- `npx vitest run` — FULL suite green (phase gate before /gsd:verify-work).
</verification>

<success_criteria>
- OVR-03 satisfied: snapshot freezes variant-tier summary override (and bullet/project overrides via builder arrays).
- D-05 satisfied: initial-run scoring builds resumeText from effectiveProfile carrying summaryOverride (forward-compat).
- Live DOCX export honors summaryOverride; snapshot-render DOCX path unchanged (already frozen).
- Full Vitest suite green.
</success_criteria>

<output>
Create `.planning/phases/36-merge-precedence-snapshot-threading/36-04-SUMMARY.md` when done.
</output>
