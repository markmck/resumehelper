---
phase: 36-merge-precedence-snapshot-threading
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/main/lib/mergeHelper.precedence.test.ts
  - tests/unit/main/handlers/templates.variantOverride.test.ts
  - tests/unit/main/handlers/submissions.snapshot.test.ts
  - tests/unit/main/handlers/ai.runAnalysis.test.ts
autonomous: true
requirements: [OVR-02, OVR-03]

must_haves:
  truths:
    - "The OVR-02 merge-precedence unit test file exists and expresses analysis-tier-wins, variant-tier-applies, project-title, summary, and D-01 inclusion cases"
    - "The D-02/D-03 variant-override handler round-trip test file exists"
    - "Existing snapshot + runAnalysis tests carry new OVR-03 / D-05 cases"
    - "All new tests are RED against the un-implemented merge/handlers (fail for the right reason: missing behavior, not missing import of a non-existent symbol)"
  artifacts:
    - path: "tests/unit/main/lib/mergeHelper.precedence.test.ts"
      provides: "OVR-02 precedence + D-01 inclusion correctness contract"
      contains: "buildMergedBuilderData"
    - path: "tests/unit/main/handlers/templates.variantOverride.test.ts"
      provides: "D-02/D-03 setVariantOverride/getVariantOverrides/clearVariantOverride round-trip"
      contains: "setVariantOverride"
    - path: "tests/unit/main/handlers/submissions.snapshot.test.ts"
      provides: "OVR-03 snapshot-freezes-override cases"
      contains: "summaryOverride"
    - path: "tests/unit/main/handlers/ai.runAnalysis.test.ts"
      provides: "D-05 effectiveProfile uses summaryOverride case"
      contains: "entityOverrides"
  key_links:
    - from: "tests/unit/main/lib/mergeHelper.precedence.test.ts"
      to: "src/main/lib/mergeHelper.ts buildMergedBuilderData"
      via: "import + call"
      pattern: "buildMergedBuilderData\\(db"
    - from: "tests/unit/main/handlers/templates.variantOverride.test.ts"
      to: "src/main/handlers/templates.ts variant-override handlers"
      via: "import of getVariantOverrides/setVariantOverride/clearVariantOverride"
      pattern: "setVariantOverride"
---

<objective>
Wave 0: author the failing correctness-contract tests BEFORE any implementation. This plan creates the two new test files the validation map requires and extends two existing test files. These tests are the executable specification for Plans 02–04.

Purpose: Lock the OVR-02 precedence contract, OVR-03 snapshot contract, and D-01/D-02/D-03/D-05 behaviors as RED tests so implementation has an unambiguous target. Satisfies the Nyquist Wave-0 gap requirement (two new test files) from 36-VALIDATION.md.
Output: 2 new test files (RED) + 2 extended test files (new RED cases).
</objective>

<execution_context>
@D:/Projects/resumeHelper/.claude/get-shit-done/workflows/execute-plan.md
@D:/Projects/resumeHelper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-merge-precedence-snapshot-threading/36-CONTEXT.md
@.planning/phases/36-merge-precedence-snapshot-threading/36-RESEARCH.md
@.planning/phases/36-merge-precedence-snapshot-threading/36-PATTERNS.md
@.planning/phases/36-merge-precedence-snapshot-threading/36-VALIDATION.md

<interfaces>
<!-- Contracts the tests assert against. Implementation in Plans 02–04. -->

entity_type / field token table (chosen, use VERBATIM in every fixture):
| Entity | entity_type | field | FK column |
| Job bullet text | 'job_bullet' | 'text' | bulletId |
| Summary | 'summary' | 'text' | (all FK columns null) |
| Project title | 'project_name' | 'name' | projectId |

source values: 'user' (variant reword), 'ai_suggestion' (analysis bullet), 'inclusion' (re-included bullet).
NULL scope: variant_id set + analysis_id NULL = variant-tier; both set = analysis-tier (wins).

buildMergedBuilderData(db, variantId, analysisId?) returns MergedBuilderData.
After Plan 02 it gains `summaryOverride?: string` (absent = use profileRow.summary).

Variant-override handlers (Plan 03, live in src/main/handlers/templates.ts):
  getVariantOverrides(db, variantId) -> raw rows { entityType, field, bulletId, projectId, overrideText, source, createdAt } (analysis_id IS NULL only)
  setVariantOverride(db, variantId, entityType, field, entityId: { bulletId?, projectId? }, text)
  clearVariantOverride(db, variantId, entityType, field, entityId: { bulletId?, projectId? })

entity_overrides schema FK columns: bulletId, projectId, jobId, projectBulletId (schema.ts:251-266).
createTestDb() already has entity_overrides + both partial unique indexes (Phase 35 — confirmed db.ts:230-257). Do NOT recreate it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create mergeHelper.precedence.test.ts (OVR-02 + D-01) — RED</name>
  <files>tests/unit/main/lib/mergeHelper.precedence.test.ts</files>
  <read_first>
    - tests/unit/main/handlers/submissions.snapshot.test.ts (createTestDb + seed + direct entity insert pattern, lines 1-126)
    - tests/unit/main/handlers/ai.runAnalysis.test.ts (describe/it/beforeEach structure; analysisResults + job_postings seeding via factories)
    - tests/helpers/factories.ts (seedJob, seedBullet, seedVariant, seedProject, updateProfile, and the analysis/job-posting seed helpers — confirm exact exported names)
    - 36-PATTERNS.md "tests/unit/main/lib/mergeHelper.precedence.test.ts" section (lines 397-456) — exact import list, direct entity_overrides insert shape, the 5 test cases
    - src/main/lib/mergeHelper.ts type MergedBuilderData (lines 54-66) and buildMergedBuilderData signature (line 68)
  </read_first>
  <action>
    Create new Vitest file importing buildMergedBuilderData from '../../../../src/main/lib/mergeHelper', createTestDb from '../../../helpers/db', seed factories, and { entityOverrides, analysisResults } (plus job-posting schema) from '../../../../src/main/db/schema'. Insert entity_overrides rows directly via db.insert(entityOverrides).values({...}).run() using the locked token table. Write five it() cases mirroring 36-PATTERNS lines 448-453:
    (1) Variant-tier bullet override applies with NO analysisId — buildMergedBuilderData(db, variantId) → jobs[0].bullets[0].text equals the variant override text (preview path; relies on Layer 2.5 firing unconditionally).
    (2) Analysis-tier override WINS — insert both a variant-tier (analysisId null, source 'user') and analysis-tier (analysisId set, source 'ai_suggestion') row for the SAME bulletId → buildMergedBuilderData(db, variantId, analysisId) → text equals the analysis override text, NOT the variant text. This is the OVR-02 precedence contract.
    (3) Project title — insert entity_type='project_name', field='name', projectId set, analysisId null → projects[0].name equals override.
    (4) Summary — insert entity_type='summary', field='text', ALL FK columns null, analysisId null → merged.summaryOverride equals the override text.
    (5) D-01 inclusion — variant-exclude a bullet (templateVariantItems excluded=true), then insert a source='inclusion' analysis-tier row for that bulletId → buildMergedBuilderData(db, variantId, analysisId) → that bullet's excluded === false; AND assert that without analysisId (base variant view) the same bullet's excluded === true (re-inclusion is analysis-scoped only).
    For analysis-tier rows, seed a real analysis_results row (FK) using the factory helper + a job_postings row (mirror 36-PATTERNS line 455). Do NOT use eq(col, null) anywhere in fixtures — set analysisId: null literally in the insert object. No production code is written in this task; tests are expected to FAIL.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/lib/mergeHelper.precedence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists and imports buildMergedBuilderData + createTestDb + entityOverrides.
    - Exactly the five cases above are present (case 2 = analysis-wins, case 5 = inclusion analysis-scoped).
    - Every fixture uses tokens job_bullet/text, summary/text (FKs null), project_name/name verbatim.
    - `npx vitest run tests/unit/main/lib/mergeHelper.precedence.test.ts` RUNS (no import/compile error) and the override-behavior assertions FAIL (RED) because merge engine is not yet implemented — failures are assertion failures, not "buildMergedBuilderData is not a function".
    - No `eq(entityOverrides.analysisId, null)` appears in the file.
  </acceptance_criteria>
  <done>New file present; suite executes; precedence/summary/inclusion assertions are RED for the correct reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create templates.variantOverride.test.ts (D-02/D-03) — RED</name>
  <files>tests/unit/main/handlers/templates.variantOverride.test.ts</files>
  <read_first>
    - tests/unit/main/handlers/ai.runAnalysis.test.ts (extracted-handler-call-without-IPC pattern; createTestDb)
    - 36-PATTERNS.md "tests/unit/main/handlers/templates.variantOverride.test.ts" section (lines 459-480) — import list + 5 test cases
    - 36-RESEARCH.md Pattern 6 (handler signatures for getVariantOverrides/setVariantOverride/clearVariantOverride)
    - src/main/handlers/templates.ts getVariantOptions/setVariantOptions (lines 40-58) for db-first extracted-function shape
  </read_first>
  <action>
    Create new Vitest file importing getVariantOverrides, setVariantOverride, clearVariantOverride from '../../../../src/main/handlers/templates', createTestDb, seed factories (seedVariant, seedJob, seedBullet, seedProject), and entityOverrides from schema. Write five it() cases (36-PATTERNS lines 475-479):
    (1) setVariantOverride(db, variantId, 'job_bullet', 'text', { bulletId }, 'New text') then getVariantOverrides(db, variantId) returns one row with entityType='job_bullet', field='text', bulletId set, overrideText='New text', source='user'.
    (2) setVariantOverride with '   ' (whitespace) DELETES — after first storing a row, calling again with whitespace yields getVariantOverrides returning empty (D-02 empty-deletes).
    (3) Calling setVariantOverride twice on the same entity replaces (no duplicate) — getVariantOverrides returns exactly one row with the second text (atomic delete+insert).
    (4) clearVariantOverride(db, variantId, 'job_bullet', 'text', { bulletId }) deletes the row → getVariantOverrides empty.
    (5) getVariantOverrides returns ONLY analysis_id IS NULL rows — directly insert an analysis-tier row (analysisId set) for the same variant and assert it is NOT returned (scope guard / no cross-tier leak).
    Tests are expected to FAIL (handlers not yet exported).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/handlers/templates.variantOverride.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File imports the three handler names from templates.ts.
    - The five cases above are present; case 2 asserts whitespace deletes, case 5 asserts analysis-tier rows are excluded from the variant read.
    - Suite executes and FAILS (RED) because the handlers are not yet exported from templates.ts.
    - source='user' and tokens used verbatim in fixtures/assertions.
  </acceptance_criteria>
  <done>New file present; suite RED on missing handler exports / behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend submissions.snapshot.test.ts (OVR-03) + ai.runAnalysis.test.ts (D-05) — RED</name>
  <files>tests/unit/main/handlers/submissions.snapshot.test.ts, tests/unit/main/handlers/ai.runAnalysis.test.ts</files>
  <read_first>
    - tests/unit/main/handlers/submissions.snapshot.test.ts (existing lines 1-126 — the db.insert(templateVariantItems).values().run() + buildSnapshotForVariant(db, variant.id) + expect(snapshot.profile!.name) pattern)
    - tests/unit/main/handlers/ai.runAnalysis.test.ts (existing seedRunAnalysisFixtures + vi.spyOn(aiProvider,'getModel').mockReturnValue(mock) + runAnalysis(db, event, posting.id, variant.id) flow)
    - 36-PATTERNS.md submissions.snapshot.test.ts section (lines 483-502) and ai.runAnalysis.test.ts section (lines 506-528)
    - 36-RESEARCH.md Pitfall 4 (do NOT assert the scorer receives summary in resumeText — buildResumeTextForLlm omits basics.summary)
  </read_first>
  <action>
    In submissions.snapshot.test.ts add three OVR-03 cases (36-PATTERNS lines 488-490): (a) insert a variant-tier entity_type='summary' override → buildSnapshotForVariant(db, variant.id) → snapshot.profile.summary equals the override text (not profileRow.summary); (b) insert a variant-tier job_bullet override → snapshot's frozen bullet text equals override; (c) baseline with no override → snapshot.profile.summary equals profileRow.summary. Use the existing direct-insert .run() pattern and import entityOverrides.
    In ai.runAnalysis.test.ts add one D-05 case (36-PATTERNS lines 512-527): seed run-analysis fixtures, insert a variant-tier entity_type='summary' override for that variant, mock the LLM, run runAnalysis, assert it completes and returns an analysisId. Per Pitfall 4, do NOT assert the scorer's resumeText contains the summary text. If feasible without brittleness, additionally assert (via a spy on buildResumeJson or by checking the merged path) that effectiveProfile.summary would equal the override — otherwise keep the assertion to successful completion + analysisId. Import entityOverrides.
    All added cases are expected to FAIL (RED) until Plan 02/04 land.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/handlers/submissions.snapshot.test.ts tests/unit/main/handlers/ai.runAnalysis.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - submissions.snapshot.test.ts contains the three OVR-03 cases (summary-override, bullet-override, baseline-no-override) and imports entityOverrides.
    - ai.runAnalysis.test.ts contains one D-05 case seeding a variant-tier summary override; it does NOT assert the scorer's resumeText includes the summary string.
    - Both suites execute; the new OVR-03 summary/bullet cases are RED (snapshot still freezes base text), and pre-existing cases remain green.
    - No `eq(col, null)`; analysisId set literally to null in variant-tier inserts.
  </acceptance_criteria>
  <done>Both existing files extended; new OVR-03 cases RED; pre-existing cases still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → in-memory SQLite | Test-only; no renderer/network input. No external trust boundary crossed in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | test fixtures using wrong NULL idiom | mitigate | Fixtures set analysisId literally null; acceptance criteria forbid eq(col, null) which would silently match nothing and produce false-green tests |
| T-36-02 | Information disclosure | cross-tier override leak in read | mitigate | Test case 5 (Task 2) asserts getVariantOverrides excludes analysis-tier rows — locks the scope-by-variantId + analysis_id IS NULL contract |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: skipped — no new packages) |
</threat_model>

<verification>
- `npx vitest run tests/unit/main/lib/mergeHelper.precedence.test.ts tests/unit/main/handlers/templates.variantOverride.test.ts` — both new files execute (compile/import clean) and assert override behavior (RED).
- `npx vitest run tests/unit/main/` — no pre-existing test regressed by the additions; only the newly added cases are RED.
</verification>

<success_criteria>
- Two new test files exist and run.
- Existing snapshot + runAnalysis tests carry the new OVR-03/D-05 cases.
- New behavioral assertions are RED for the right reason (missing implementation, not missing symbols/compile errors).
- 36-VALIDATION.md Wave 0 gaps (2 new files) are now satisfiable; nyquist_compliant becomes achievable once Plans 02–04 turn them green.
</success_criteria>

<output>
Create `.planning/phases/36-merge-precedence-snapshot-threading/36-01-SUMMARY.md` when done.
</output>
