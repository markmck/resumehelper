---
phase: 35-unified-override-table-migration
plan: 03
type: execute
wave: 3
depends_on: ["35-01", "35-02"]
files_modified:
  - src/main/handlers/ai.ts
  - src/main/lib/mergeHelper.ts
  - tests/data/acceptSuggestion.test.ts
  - .planning/PROJECT.md
autonomous: true
requirements: [OVR-01]

must_haves:
  truths:
    - "acceptSuggestion writes an analysis-tier row to entity_overrides (not analysis_bullet_overrides)"
    - "dismissSuggestion deletes the matching entity_overrides job_bullet row"
    - "getOverrides reads accepted bullet overrides from entity_overrides"
    - "mergeHelper Layer 3 reads accepted bullet overrides from entity_overrides so a freshly accepted suggestion renders in preview (no regression — ROADMAP #3)"
    - "The D-01 per-entity-FK schema decision is recorded in PROJECT.md Key Decisions"
  artifacts:
    - path: "src/main/handlers/ai.ts"
      provides: "acceptSuggestion/dismissSuggestion/getOverrides cut over to entity_overrides"
      contains: "entityOverrides"
    - path: "src/main/lib/mergeHelper.ts"
      provides: "Layer 3 reads job_bullet overrides from entity_overrides"
      contains: "entityOverrides"
    - path: ".planning/PROJECT.md"
      provides: "D-01 decision row in Key Decisions table"
      contains: "per-entity nullable FK"
  key_links:
    - from: "src/main/handlers/ai.ts acceptSuggestion"
      to: "entity_overrides (analysis-tier, entity_type='job_bullet')"
      via: "db.insert(entityOverrides).onConflictDoUpdate"
      pattern: "entityOverrides"
    - from: "src/main/lib/mergeHelper.ts Layer 3"
      to: "entity_overrides"
      via: "db.select().from(entityOverrides) filtered to analysisId + job_bullet"
      pattern: "entityOverrides"
---

<objective>
Cut the single existing override writer over to `entity_overrides`, and redirect the matching bullet read path so an accepted suggestion appears in preview without regression. This resolves the load-bearing CONTEXT.md-vs-ARCHITECTURE.md tension: Phase 35 does a SCOPED read+write cutover for the one entity type that has a writer (`job_bullet`), while the full multi-field/variant-tier merge precedence stays in Phase 36.

Three handler functions cut over (`acceptSuggestion`, `dismissSuggestion`, `getOverrides`), one merge read shim (mergeHelper Layer 3), and the D-01 decision is recorded in PROJECT.md.

Purpose: ROADMAP success criterion #3 (accepted text appears in preview without regression) + #5 (schema decision recorded). Single source of truth — no dual-write (D-06).
Output: entity_overrides is the sole read+write path for accepted bullet rewrites; PROJECT.md documents the FK design.
</objective>

<execution_context>
@D:/Projects/resumeHelper/.claude/get-shit-done/workflows/execute-plan.md
@D:/Projects/resumeHelper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-unified-override-table-migration/35-CONTEXT.md
@.planning/phases/35-unified-override-table-migration/35-PATTERNS.md

<decisions_in_effect>
- D-06: ATOMIC CUTOVER, NO DUAL-WRITE. acceptSuggestion writes ONLY to entity_overrides as an analysis-tier row: variant_id resolved from the analysis's variant, analysis_id set, entity_type='job_bullet', field='text', bullet_id set, source='ai_suggestion'. The old analysis_bullet_overrides table is frozen read-only — never written again after cutover.
- RESOLVED — Read-path / ROADMAP #3 tension: SCOPED read+write cutover. Because acceptSuggestion now writes to entity_overrides while mergeHelper Layer 3 still reads analysis_bullet_overrides, a freshly accepted suggestion would NOT surface in preview = regression. Fix: redirect mergeHelper Layer 3 AND getOverrides to read job_bullet overrides from entity_overrides (filtered to analysisId, entity_type='job_bullet'). This is the MINIMAL scoped redirect for the one entity type with a writer — NOT the full Phase 36 work (no variant-tier reads, no summary/project/job multi-field application, no inclusion un-exclusion). Phase 36 generalizes it.
- RESOLVED — Uniqueness (from Plan 01): analysis-tier partial unique index `entity_overrides_analysis_tier_uidx` on (analysis_id, entity_type, bullet_id, project_id, job_id, project_bullet_id, field) WHERE analysis_id IS NOT NULL. acceptSuggestion's onConflictDoUpdate target MUST match these index columns.
- D-01 must be recorded in PROJECT.md Key Decisions this phase (ROADMAP #5 / STATE.md blocker).
</decisions_in_effect>

<interfaces>
<!-- Current acceptSuggestion (ai.ts:134-153) — the cutover point -->
acceptSuggestion(db, analysisId, bulletId, text): db.insert(analysisBulletOverrides).values({analysisId, bulletId, overrideText: text, source: 'ai_suggestion'}).onConflictDoUpdate({target: [analysisBulletOverrides.analysisId, analysisBulletOverrides.bulletId], set: {...}}).run()

<!-- variant lookup idiom already used in this file (ai.ts:14-30 style) -->
db.select({ variantId: analysisResults.variantId }).from(analysisResults).where(eq(analysisResults.id, analysisId)).get()

<!-- Current dismissSuggestion (ai.ts:155-170) -->
db.delete(analysisBulletOverrides).where(and(eq(analysisBulletOverrides.analysisId, analysisId), eq(analysisBulletOverrides.bulletId, bulletId))).run()

<!-- Current getOverrides (ai.ts:172-196) — raw sqlite.prepare LEFT JOIN job_bullets for isOrphaned; returns {bulletId, overrideText, source, suggestionId, isOrphaned} -->

<!-- Current mergeHelper Layer 3 (mergeHelper.ts:274-298) -->
const overrideRows = db.select({bulletId: analysisBulletOverrides.bulletId, overrideText: ..., source: ..., suggestionId: ...}).from(analysisBulletOverrides).where(eq(analysisBulletOverrides.analysisId, analysisId)).all()
for (const job of jobsWithBullets) { job.bullets = applyOverrides(job.bullets, overrideRows) }
<!-- applyOverrides (src/shared/overrides.ts) maps BulletOverride[] {bulletId, overrideText} onto bullets by id -->

<!-- IPC channel names UNCHANGED: ai:acceptSuggestion, ai:dismissSuggestion, ai:getOverrides (ai.ts:264-274) -->
<!-- entity_overrides has NO suggestion_id column — getOverrides must return suggestionId: null (the column does not exist on the new table) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cut acceptSuggestion + dismissSuggestion + getOverrides over to entity_overrides</name>
  <read_first>
    - src/main/handlers/ai.ts:1-12 (imports — add entityOverrides to the ../db/schema barrel; isNull from drizzle-orm may be needed)
    - src/main/handlers/ai.ts:134-196 (the three functions to cut over: acceptSuggestion, dismissSuggestion, getOverrides)
    - src/main/db/schema.ts (entityOverrides definition from Plan 01 — exact column names; analysisResults.variantId for the lookup)
    - tests/data (existing handler test style — locate any existing ai.ts handler test that uses createTestDb to copy fixture setup)
  </read_first>
  <behavior>
    - Test 1 (accept writes analysis-tier row): seed analysis_results(id=1, variant_id=7) and a job_bullet(id=42). Call acceptSuggestion(db, 1, 42, 'New text'). entity_overrides has one row: analysis_id=1, variant_id=7, entity_type='job_bullet', field='text', bullet_id=42, override_text='New text', source='ai_suggestion'. analysis_bullet_overrides has ZERO rows for that pair (no dual-write).
    - Test 2 (accept is upsert): call acceptSuggestion twice for same (1,42) with different text; entity_overrides still has exactly one row, override_text reflects the second call (onConflictDoUpdate on the analysis-tier index).
    - Test 3 (dismiss deletes): after accept, dismissSuggestion(db, 1, 42) removes the entity_overrides job_bullet row for that analysis_id+bullet_id; row count for that pair is 0.
    - Test 4 (getOverrides reads new table): after accept, getOverrides(db, 1) returns an array containing {bulletId:42, overrideText:'New text', source:'ai_suggestion', suggestionId:null, isOrphaned:false}. Delete the bullet → isOrphaned:true.
    - Test 5 (return contract): acceptSuggestion returns {success:true} on success; an error path returns {error: string} (contract unchanged).
  </behavior>
  <action>
    Add `entityOverrides` to the `../db/schema` import in ai.ts (keep analysisBulletOverrides imported — getOverrides may still reference it elsewhere; but the three functions below no longer write/read it). Add `isNull` to the drizzle-orm import if needed.
    - acceptSuggestion(db, analysisId, bulletId, text): FIRST resolve variantId via `db.select({ variantId: analysisResults.variantId }).from(analysisResults).where(eq(analysisResults.id, analysisId)).get()`. Then `db.insert(entityOverrides).values({ variantId, analysisId, entityType: 'job_bullet', field: 'text', bulletId, overrideText: text, source: 'ai_suggestion' }).onConflictDoUpdate({ target: [entityOverrides.analysisId, entityOverrides.entityType, entityOverrides.bulletId, entityOverrides.projectId, entityOverrides.jobId, entityOverrides.projectBulletId, entityOverrides.field], set: { overrideText: text, source: 'ai_suggestion' } }).run()`. The target column set MUST match the analysis-tier partial unique index from Plan 01 (analysis_id + entity_type + all four FK cols + field). Keep the existing try/catch → {success}/{error} contract verbatim.
    - dismissSuggestion(db, analysisId, bulletId): `db.delete(entityOverrides).where(and(eq(entityOverrides.analysisId, analysisId), eq(entityOverrides.entityType, 'job_bullet'), eq(entityOverrides.bulletId, bulletId))).run()`. Same contract.
    - getOverrides(db, analysisId): redirect the raw read to entity_overrides. Read job_bullet rows for this analysis (analysis_id = ?, entity_type='job_bullet') with a LEFT JOIN job_bullets for isOrphaned, mapping to {bulletId, overrideText, source, suggestionId: null, isOrphaned}. entity_overrides has NO suggestion_id column, so suggestionId is always null. Use parameterized SQL (prepare(...).all(analysisId)) or Drizzle — no string-concatenated input.
    IPC registrations (ai.ts:264-274) stay UNCHANGED (channel names identical). Do NOT write to analysis_bullet_overrides anywhere in these three functions (D-06 — no dual-write).
    Create/extend `tests/data/acceptSuggestion.test.ts` implementing the five behaviors using createTestDb().
  </action>
  <verify>
    <automated>npm test -- tests/data/acceptSuggestion.test.ts 2>&1 | tail -25; npx tsc --noEmit -p tsconfig.node.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "analysisBulletOverrides" src/main/handlers/ai.ts` shows the three cut-over functions no longer insert/delete/read analysisBulletOverrides (acceptSuggestion/dismissSuggestion/getOverrides bodies reference entityOverrides instead — confirm by source inspection of each function body).
    - acceptSuggestion inserts into entityOverrides with entityType:'job_bullet', field:'text', and a resolved variantId (Test 1 asserts variant_id=7, bullet_id=42).
    - The onConflictDoUpdate target columns match the analysis-tier partial unique index (analysisId, entityType, bulletId, projectId, jobId, projectBulletId, field) — source inspection.
    - No dual-write: after acceptSuggestion, analysis_bullet_overrides has zero rows for the accepted pair (Test 1).
    - getOverrides returns suggestionId:null for new-table rows and sets isOrphaned correctly (Test 4).
    - All five tests in tests/data/acceptSuggestion.test.ts pass; `npx tsc --noEmit -p tsconfig.node.json` exits 0.
  </acceptance_criteria>
  <done>acceptSuggestion/dismissSuggestion/getOverrides operate solely on entity_overrides; no dual-write; five tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redirect mergeHelper Layer 3 bullet read to entity_overrides (scoped, no-regression)</name>
  <read_first>
    - src/main/lib/mergeHelper.ts:274-298 (Layer 3 — the analysisBulletOverrides read + applyOverrides call to redirect)
    - src/shared/overrides.ts (applyOverrides signature + BulletOverride shape — the redirect must produce the same {bulletId, overrideText} shape it consumes)
    - src/main/db/schema.ts (entityOverrides columns from Plan 01)
  </read_first>
  <behavior>
    - Test 1 (accepted suggestion renders): seed analysis_results(id=1, variant_id=7), a job + job_bullet(id=42, text='Base'), and a variant that includes the bullet. Call acceptSuggestion(db, 1, 42, 'Accepted text'). Then buildMergedBuilderData(db, 7, 1): the bullet with id=42 has text='Accepted text' (NOT 'Base') — proves ROADMAP #3 no-regression end-to-end through the new table.
    - Test 2 (no analysisId = no override applied): buildMergedBuilderData(db, 7) with no analysisId leaves bullet 42 as 'Base' (Layer 3 only runs when analysisId != null — unchanged behavior).
    - Test 3 (migrated row still renders): insert directly into entity_overrides a migrated-style row (analysis_id=1, entity_type='job_bullet', bullet_id=42, override_text='Migrated', source='ai_suggestion', variant_id=7); buildMergedBuilderData(db,7,1) renders 'Migrated' — proves migrated data (Plan 02) is readable.
  </behavior>
  <action>
    In mergeHelper.ts Layer 3 (the `if (analysisId != null)` block at ~274-298), replace the `db.select(...).from(analysisBulletOverrides).where(eq(analysisBulletOverrides.analysisId, analysisId))` query with a read from entityOverrides filtered to: `eq(entityOverrides.analysisId, analysisId)` AND `eq(entityOverrides.entityType, 'job_bullet')`. Select `bulletId` and `overrideText` (and source/suggestionId as available; suggestionId is null on the new table). Map the result rows into the `BulletOverride[]` shape that `applyOverrides` already consumes ({bulletId, overrideText, source, suggestionId}) so the existing `applyOverrides(job.bullets, overrideRows)` call site is unchanged. Update the import to include `entityOverrides` from '../db/schema'. Keep this SCOPED — do NOT add variant-tier reads (isNull(analysisId) rows), summary/project/job field application, or inclusion un-exclusion; those are Phase 36. The skill-additions Layer 3 block (mergeHelper.ts:300+) is unchanged.
    Add the three behaviors to a merge test (extend an existing mergeHelper test file under tests/, or add tests/data/mergeOverrides.test.ts) using createTestDb().
  </action>
  <verify>
    <automated>npm test -- mergeHelper mergeOverrides 2>&1 | tail -25; npx tsc --noEmit -p tsconfig.node.json</automated>
  </verify>
  <acceptance_criteria>
    - mergeHelper.ts Layer 3 reads from entityOverrides filtered by `eq(entityOverrides.entityType, 'job_bullet')` and `eq(entityOverrides.analysisId, analysisId)` (source inspection).
    - The applyOverrides call site shape is preserved (overrideRows still {bulletId, overrideText, ...}).
    - Test 1 proves an accepted suggestion renders overridden text via buildMergedBuilderData — the no-regression acceptance for ROADMAP #3.
    - Test 3 proves a migrated-style entity_overrides row renders (Plan 02 data is readable through merge).
    - No variant-tier / multi-field / inclusion logic added (Phase 36 scope) — confirm the block still gates on `analysisId != null` and only handles job_bullet text.
    - `npm test` full suite passes ≥247 tests; `npx tsc --noEmit -p tsconfig.node.json` exits 0.
  </acceptance_criteria>
  <done>Layer 3 reads bullet overrides from entity_overrides; accepted + migrated rows both render in preview; no Phase 36 scope creep; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Record the D-01 per-entity-FK schema decision in PROJECT.md Key Decisions</name>
  <read_first>
    - .planning/PROJECT.md:149-164 (the Key Decisions table — append a new row in the same `| Decision | Rationale | Outcome |` format)
    - .planning/phases/35-unified-override-table-migration/35-CONTEXT.md (D-01 text — the exact rationale to record)
  </read_first>
  <action>
    Append one row to the Key Decisions table in PROJECT.md (after the last existing row), documenting D-01: Decision = "Unified entity_overrides uses per-entity nullable FK columns (bullet_id, project_id, job_id, project_bullet_id) + entity_type/field discriminators, NOT a generic entity_id"; Rationale = "Matches template_variant_items precedent; only shape where SQLite ON DELETE CASCADE fires per parent table — a generic entity_id would orphan override rows on parent delete"; Outcome = "✓ Good" (or "— Phase 35"). This satisfies ROADMAP success criterion #5 and clears the STATE.md blocker note. Do NOT alter any existing decision rows.
  </action>
  <verify>
    <automated>grep -c "per-entity nullable FK" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - PROJECT.md Key Decisions table contains a new row mentioning "per-entity nullable FK" and "entity_overrides" (or "generic entity_id" rationale).
    - `grep -c "per-entity nullable FK" .planning/PROJECT.md` returns at least 1.
    - The existing decision rows (lines 155-164) are unchanged (no edits to prior rows).
  </acceptance_criteria>
  <done>D-01 recorded in PROJECT.md Key Decisions; ROADMAP #5 satisfied; STATE.md blocker cleared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer (IPC) → ai.ts handlers | analysisId/bulletId/text arrive from the renderer over IPC into acceptSuggestion/dismissSuggestion/getOverrides |
| handler → entity_overrides write | accepted suggestion text is persisted to the local DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07 | Injection | acceptSuggestion / dismissSuggestion / getOverrides writes | mitigate | All writes use parameterized Drizzle queries (`db.insert/.delete/.select`) or parameterized `prepare(...).all(analysisId)`; no string-concatenated SQL. text/bulletId never interpolated into a SQL string. |
| T-35-08 | Tampering | dual-write / source-of-truth | mitigate | D-06 single-write: handlers write ONLY entity_overrides; analysis_bullet_overrides is never written post-cutover, preventing divergent sources of truth (Anti-Pattern 1). Verified by Test 1 asserting zero old-table rows. |
| T-35-09 | Spoofing / referential integrity | accept with stale bulletId/analysisId | accept | A hallucinated/invalid bulletId would write a row whose FK cascade simply never matches a live bullet (getOverrides flags isOrphaned). Full validate-before-write is Phase 38's acceptExcludedBulletSuggestion concern, not this migrated rewrite path. Low risk: the rewrite UI only offers real bullet IDs from the current analysis. |
| T-35-SC | Tampering | npm installs | mitigate | This plan adds NO new packages. If an install is attempted, stop and route to a blocking-human legitimacy checkpoint. |
</threat_model>

<verification>
- `npm test -- tests/data/acceptSuggestion.test.ts` (5 tests) and merge tests (3 tests) pass.
- Full suite ≥247 tests, zero failures.
- `npx tsc --noEmit -p tsconfig.node.json` succeeds.
- PROJECT.md Key Decisions records D-01.
</verification>

<success_criteria>
- ROADMAP #3: accepting a bullet rewrite writes to entity_overrides AND the accepted text appears in preview (Task 1 + Task 2 Test 1) — no regression.
- ROADMAP #5: D-01 schema decision recorded (Task 3).
- D-06: no dual-write — single source of truth.
- No Phase 36 scope creep (scoped job_bullet read shim only).
</success_criteria>

<output>
Create `.planning/phases/35-unified-override-table-migration/35-03-SUMMARY.md` when done.
</output>
