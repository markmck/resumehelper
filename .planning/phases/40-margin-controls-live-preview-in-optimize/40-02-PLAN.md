---
phase: 40-margin-controls-live-preview-in-optimize
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - src/renderer/src/components/OptimizeVariant.tsx
autonomous: true
requirements: [LAYOUT-01, LAYOUT-05, LAYOUT-08]
must_haves:
  truths:
    - "The Optimize screen shows three margin sliders (Top, Bottom, Sides) seeded with the analysis's effective margins"
    - "D-02: The sliders reuse VariantBuilder's config — range 0.4″–1.2″, step 0.05″, with the amber readout when a value is < 0.5″"
    - "Dragging a slider updates the embedded paginated preview live and persists the analysis override with no separate save button"
    - "D-06: The preview uses the existing PrintApp paged-box rendering as-is (visible page boxes); no pageCount-bubbling or overflow-warning styling is added in this phase (deferred to Phase 41)"
    - "The preview reflects the analysis's full merged content — re-included excluded bullets and accepted rewrites are visible"
    - "An always-rendered 'Revert to variant margins' control clears the override and the preview/page-count updates; it is disabled when no override exists"
  artifacts:
    - path: "src/renderer/src/components/OptimizeVariant.tsx"
      provides: "MARGINS collapsible section (3 sliders + revert) wired to Phase 39 IPC, with margin props passed to VariantPreview"
      contains: "MARGIN_FLOOR"
  key_links:
    - from: "OptimizeVariant slider onInput"
      to: "local margin state → VariantPreview marginTop/marginBottom/marginSides props"
      via: "synchronous local state read (instant preview)"
      pattern: "marginTop=\\{"
    - from: "OptimizeVariant debounced timer"
      to: "window.api.analysisLayout.setMargins(analysisId, triple)"
      via: "~300ms setTimeout ref, full triple written each time (D-01/D-04)"
      pattern: "analysisLayout\\.setMargins"
    - from: "OptimizeVariant revert control"
      to: "window.api.analysisLayout.clearMargins(analysisId)"
      via: "clear → re-seed from variant margins → bump refreshKey (D-05)"
      pattern: "analysisLayout\\.clearMargins"
    - from: "seed-on-mount"
      to: "window.api.analysisLayout.getMargins(analysisId) → BuilderData.effectiveMargins fallback"
      via: "override row if present, else pre-resolved effectiveMargins"
      pattern: "analysisLayout\\.getMargins"
---

<objective>
Wire the analysis-scoped margin UI into the Optimize screen: three Top/Bottom/Sides sliders in a collapsible MARGINS section, seeded from the analysis's effective margins, with live preview updates, a ~300ms debounced persist to the Phase 39 override path, and an always-rendered revert-to-variant control.

Purpose: Delivers all three Phase 40 requirements (LAYOUT-01 adjust margins, LAYOUT-05 live paginated preview, LAYOUT-08 revert). The preview is the proof — because it renders merged content (re-included bullets + accepted rewrites) at the chosen margins, the user can watch the resume spill onto / pull back from an extra page as they drag, which is the whole point of v2.7 and the direct setup for Phase 41 auto-fit.
Output: Modified `src/renderer/src/components/OptimizeVariant.tsx` only. No backend, no new IPC — consumes the Phase 39 `analysisLayout` bridge and the Plan 01 foundation (`MARGIN_FLOOR`, `BuilderData.effectiveMargins`).

Binding contract: this plan implements the APPROVED 40-UI-SPEC.md exactly (slider bounds, amber cue, collapsed-summary format, revert enabled/disabled states, placement above Live Preview). Treat the UI-SPEC as non-negotiable.
</objective>

<execution_context>
@D:/Projects/resumeHelper/.claude/get-shit-done/workflows/execute-plan.md
@D:/Projects/resumeHelper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-margin-controls-live-preview-in-optimize/40-CONTEXT.md
@.planning/phases/40-margin-controls-live-preview-in-optimize/40-PATTERNS.md
@.planning/phases/40-margin-controls-live-preview-in-optimize/40-UI-SPEC.md

<interfaces>
<!-- Everything the executor needs — no codebase scavenger hunt required. -->

Plan 01 foundation (must be merged first — this plan's depends_on):
```typescript
// src/renderer/src/lib/marginConstants.ts
export const MARGIN_FLOOR = 0.4 // inches

// src/preload/index.d.ts — BuilderData now has:
effectiveMargins?: { top: number; bottom: number; sides: number }
```

Phase 39 IPC bridge (already shipped — src/preload/index.d.ts:648-652):
```typescript
analysisLayout: {
  getMargins: (analysisId: number) => Promise<{ id: number; analysisId: number; marginTop: number; marginBottom: number; marginSides: number } | null>
  setMargins: (analysisId: number, margins: { marginTop: number; marginBottom: number; marginSides: number }) => Promise<void>
  clearMargins: (analysisId: number) => Promise<void>
}
```

VariantPreview already accepts these props (src/renderer/src/components/VariantPreview.tsx:12-14, 81-84) —
no change to that file:
```typescript
marginTop?: number
marginBottom?: number
marginSides?: number
```

AnalysisData available fields in OptimizeVariant (src/renderer/src/components/OptimizeVariant.tsx:28-45):
`analysis.id` (the analysisId), `analysis.variantId`. NOTE: AnalysisData has NO layoutTemplate field —
this is exactly why seeding reads the pre-resolved `effectiveMargins` from BuilderData rather than
resolving TEMPLATE_DEFAULTS[template] in the renderer.

Existing debounce pattern to copy (OptimizeVariant.tsx:139-157):
useRef<ReturnType<typeof setTimeout> | null>(null) + clearTimeout before each setTimeout(..., 300)
+ cleanup useEffect on unmount.

Existing Live Preview JSX to extend (OptimizeVariant.tsx:1818-1845): the `<VariantPreview>` call
currently passes only variantId / analysisId / refreshKey. The new MARGINS section goes ABOVE this block.

Existing preview refresh key state (OptimizeVariant.tsx:135): `previewRefreshKey` / `setPreviewRefreshKey`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Margin local state, effective-margins seed, debounced persist, and revert logic</name>
  <files>src/renderer/src/components/OptimizeVariant.tsx</files>
  <read_first>
    - src/renderer/src/components/OptimizeVariant.tsx §28-45 (AnalysisData type — confirm only id/variantId available, no layoutTemplate), §128-157 (threshold debounce + cleanup pattern to mirror), §159-200 (the on-mount load useEffect where the seed call belongs)
    - src/renderer/src/lib/marginConstants.ts (Plan 01 — the MARGIN_FLOOR you will import)
    - src/preload/index.d.ts §155-168 (BuilderData.effectiveMargins shape), §648-652 (analysisLayout bridge signatures)
    - .planning/phases/40-margin-controls-live-preview-in-optimize/40-PATTERNS.md §"Debounce-to-IPC pattern", §"Seed-on-mount pattern", §"Revert control"
    - .planning/phases/40-margin-controls-live-preview-in-optimize/40-UI-SPEC.md §"Seeding on Mount", §"Revert Control" (binding)
  </read_first>
  <action>
    Add to OptimizeVariant: (1) Local margin state — three numbers `marginTop` / `marginBottom` / `marginSides` (init from MARGIN_FLOOR-safe defaults, real values arrive from seed) plus a `hasOverride: boolean` state. Import `MARGIN_FLOOR` from `../lib/marginConstants`.
    (2) Seed-on-mount: inside the existing on-mount load flow (where `analysis.id`/`variantId` become available), call `window.api.analysisLayout.getMargins(analysis.id)`. If it returns non-null, seed the three state values from `result.marginTop/marginBottom/marginSides` and set `hasOverride(true)`. If null, seed from the `effectiveMargins` returned by `window.api.templates.getBuilderData(variantId, analysisId)` (`builderData.effectiveMargins?.top/.bottom/.sides`) and set `hasOverride(false)`. If both are unavailable (IPC failure), fall back silently to a safe default triple (no user-facing error per UI-SPEC error-state row). Re-run when `analysis.id` changes.
    (3) Debounced persist: create `saveMarginsRef = useRef<ReturnType<typeof setTimeout> | null>(null)` mirroring the existing `saveThresholdRef` pattern. A `handleMarginChange(field: 'top'|'bottom'|'sides', value: number)` updates the matching local state, sets `hasOverride(true)`, then clears+resets the timer to fire `window.api.analysisLayout.setMargins(analysis.id, { marginTop, marginBottom, marginSides })` with the FULL CURRENT TRIPLE after ~300ms (D-01/D-04 — never a partial write). Add the same unmount cleanup `useEffect` clearing `saveMarginsRef`.
    (4) Revert: `handleRevert` calls `await window.api.analysisLayout.clearMargins(analysis.id)`, then ALWAYS re-fetches `window.api.templates.getBuilderData(variantId, analysis.id)` and re-seeds the three slider state values from the freshly-returned `builderData.effectiveMargins?.top/.bottom/.sides`. The resolver runs at IPC call time, so after `clearMargins` the override row is gone and `effectiveMargins` now reflects the variant margins. Do NOT re-use a cached effective triple captured at mount — it can be stale if variant margins were changed elsewhere before revert. After re-seeding, bump `setPreviewRefreshKey((k) => k + 1)` (D-05) and set `hasOverride(false)`.
    Do NOT render any JSX in this task — that is Task 2. This task only adds state, effects, and handlers.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `import { MARGIN_FLOOR } from '../lib/marginConstants'` present in OptimizeVariant.tsx
    - Source assertion: a `saveMarginsRef` useRef and a `handleMarginChange` callback exist; `handleMarginChange` calls `window.api.analysisLayout.setMargins(` with all three of `marginTop`, `marginBottom`, `marginSides` in the payload
    - Source assertion: seed logic calls `window.api.analysisLayout.getMargins(` and falls back to `effectiveMargins` from `getBuilderData` when the override is null
    - Source assertion: `handleRevert` calls `window.api.analysisLayout.clearMargins(` and `setPreviewRefreshKey(`
    - Source assertion: `handleRevert` re-fetches `window.api.templates.getBuilderData(` AFTER `clearMargins` (re-seed from fresh `effectiveMargins`, not a cached triple)
    - Source assertion: an unmount cleanup `useEffect` clears `saveMarginsRef`
    - Behavior assertion: `npm run typecheck` passes
  </acceptance_criteria>
  <done>State, seed, debounced setMargins, and revert handlers exist and typecheck; no UI rendered yet.</done>
</task>

<task type="auto">
  <name>Task 2: Render the MARGINS collapsible section and pass margin props to the preview</name>
  <files>src/renderer/src/components/OptimizeVariant.tsx</files>
  <read_first>
    - src/renderer/src/components/OptimizeVariant.tsx §1818-1845 (the existing Live Preview block — insert the MARGINS section directly above it, wire margin props into the existing VariantPreview call)
    - .planning/phases/40-margin-controls-live-preview-in-optimize/40-PATTERNS.md §"Collapsible section header pattern", §"Three-slider expanded block pattern", §"Range slider row layout", §"Amber low-margin warning", §"Passing margin props to VariantPreview" (concrete JSX to adapt)
    - .planning/phases/40-margin-controls-live-preview-in-optimize/40-UI-SPEC.md §"Margin Sliders", §"Section Disclosure", §"Revert Control", §"Live Preview Pane", §"Copywriting Contract" (binding visual/copy contract)
  </read_first>
  <action>
    Insert a new collapsible MARGINS section in OptimizeVariant immediately ABOVE the existing `{/* ── Live Preview */}` block at ~line 1818, separated with `borderTop: '1px solid var(--color-border-subtle)'` and `paddingTop: 'var(--space-4)'` (matching the existing separator styling). Add a `marginsOpen` boolean state initialised to `true` (section starts expanded, mirroring VariantBuilder default).
    Header: clickable row toggling `marginsOpen`, chevron `▼`/`▶`, uppercase `MARGINS` label (font-size-xs / weight 600 / letter-spacing 0.05em / color var(--color-text-tertiary)). When collapsed, show the inline monospace summary `{marginTop.toFixed(2)}" / {marginBottom.toFixed(2)}" / {marginSides.toFixed(2)}"`.
    Expanded body (`marginsOpen` only): three slider rows (Top, Bottom, Sides) using the exact row layout `[label minWidth 48] [range flex:1] [readout minWidth 40, right-aligned]`, row height 28, gap 8. Each `<input type="range">` uses `min={MARGIN_FLOOR}` `max={1.2}` `step={0.05}`, `onInput` (NOT onChange) calling `handleMarginChange('top'|'bottom'|'sides', parseFloat(...))`, `accentColor: '#8b5cf6'`. Each readout uses `var(--font-mono)` and turns amber `'#f59e0b'` when its value `< 0.5` (strictly less-than).
    Revert control: ALWAYS rendered as a `<span>` reading `Revert to variant margins` (static label). When `hasOverride` is true → `cursor: pointer`, `text-decoration: underline`, `opacity: 1`, `onClick={handleRevert}`. When false → `cursor: default`, no underline, `opacity: 0.4`, no onClick. Use `color: var(--color-text-muted)` in both states.
    Finally, add `marginTop={marginTop}` `marginBottom={marginBottom}` `marginSides={marginSides}` props to the EXISTING `<VariantPreview>` call in the Live Preview block (sourced from local slider state — instant preview, no debounce on the preview path per UI-SPEC). Do NOT change anything else in the Live Preview block; the existing `refreshKey={previewRefreshKey}` continues to drive revert-triggered reloads.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint -- src/renderer/src/components/OptimizeVariant.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: three `<input type="range"` controls exist with `min={MARGIN_FLOOR}`, `max={1.2}`, `step={0.05}`, and `onInput` handlers (not onChange)
    - Source assertion: the `<VariantPreview>` call now passes `marginTop`, `marginBottom`, and `marginSides` props
    - Source assertion: the revert `<span>` has the literal text `Revert to variant margins`, is rendered unconditionally, and switches `cursor`/`opacity`/`text-decoration` on `hasOverride`
    - Source assertion: each readout span applies `#f59e0b` when its value `< 0.5`
    - Source assertion: a `marginsOpen` toggle drives a chevron and shows the collapsed `.toFixed(2)" / .toFixed(2)" / .toFixed(2)"` summary
    - Behavior assertion: `npm run typecheck` passes and lint is clean for the file
  </acceptance_criteria>
  <done>The MARGINS section renders above Live Preview, sliders drive both local state and the preview, the revert control reflects override state, and typecheck/lint pass. All four ROADMAP success criteria are satisfiable by manual verification (slider seed, live drag persist, merged-content preview, revert).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC) | The margin triple sent to `analysisLayout.setMargins` crosses from renderer into the main process. It is the only untrusted input this phase introduces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02 | Tampering | margin triple → `analysisLayout.setMargins` | mitigate | Values originate from `<input type="range">` bounded by `min={MARGIN_FLOOR}` (0.4) / `max={1.2}` / `step={0.05}`; `parseFloat` produces finite numbers. No free-text numeric entry path exists. The Phase 39 handler additionally rejects non-finite input (D-09), so the renderer cannot push unbounded values into the DB. |
| T-40-03 | Denial of Service | rapid slider drag → IPC flood | mitigate | The ~300ms debounce (D-01) collapses a drag gesture into a single settled DB write rather than one write per tick, bounding IPC/DB write rate. |
| T-40-04 | Information Disclosure | preview iframe content | accept | The preview renders the user's own merged resume content already shown elsewhere in the app; no new exposure. |
| T-40-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — renderer-only changes against existing dependencies; nothing to verify. |
</threat_model>

<verification>
- `npm run typecheck` passes.
- `npm run lint` clean for OptimizeVariant.tsx.
- Human-verify checkpoint confirms: seed, live preview update, debounced persistence, merged content visible, revert behavior, collapsed summary.
- Slider `min` is `MARGIN_FLOOR` (no `0.4` literal), so the manual floor tracks the shared constant.
</verification>

<success_criteria>
- LAYOUT-01: Three Top/Bottom/Sides sliders adjust the active analysis's margins, seeded from effective margins.
- LAYOUT-05: Live paginated preview updates as margins change and reflects the analysis's merged content (re-included bullets + accepted rewrites).
- LAYOUT-08: Always-rendered revert control returns margins to the variant's and updates the preview; disabled when no override exists.
- Persistence is debounced (~300ms) via the Phase 39 `setMargins` path with the full triple; no save button.
</success_criteria>

<output>
Create `.planning/phases/40-margin-controls-live-preview-in-optimize/40-02-SUMMARY.md` when done.
</output>
