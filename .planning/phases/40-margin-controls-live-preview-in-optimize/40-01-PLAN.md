---
phase: 40-margin-controls-live-preview-in-optimize
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/lib/marginConstants.ts
  - src/preload/index.d.ts
autonomous: true
requirements: [LAYOUT-01]
must_haves:
  truths:
    - "A single MARGIN_FLOOR constant exists and is importable by both the Optimize sliders (Phase 40) and the future auto-fit clamp (Phase 41)"
    - "The renderer-facing BuilderData type exposes the pre-resolved effective margin triple so the Optimize sliders can seed from one value without re-implementing precedence"
  artifacts:
    - path: "src/renderer/src/lib/marginConstants.ts"
      provides: "MARGIN_FLOOR = 0.4 shared constant"
      contains: "export const MARGIN_FLOOR"
    - path: "src/preload/index.d.ts"
      provides: "effectiveMargins optional field on BuilderData interface"
      contains: "effectiveMargins"
  key_links:
    - from: "src/preload/index.d.ts BuilderData.effectiveMargins"
      to: "src/main/lib/mergeHelper.ts MergedBuilderData.effectiveMargins"
      via: "templates:getBuilderData IPC already serialises the full MergedBuilderData"
      pattern: "effectiveMargins"
---

<objective>
Create the two small foundation seams the Phase 40 margin UI consumes: a shared `MARGIN_FLOOR` constant and the `effectiveMargins` field on the renderer-facing `BuilderData` type.

Purpose: Both are prerequisites for the OptimizeVariant slider wiring (Plan 02). The constant guarantees the manual-drag floor and the Phase 41 auto-fit clamp can never drift (CONTEXT D-03). Exposing `effectiveMargins` on `BuilderData` lets the renderer seed sliders from one pre-resolved triple instead of re-implementing the three-tier precedence — the recommended single-source path (D-07), confirmed cheap because `MergedBuilderData` already carries the field and the `templates:getBuilderData` handler already serialises it over IPC.
Output: `src/renderer/src/lib/marginConstants.ts` (new) and a one-line addition to the `BuilderData` interface in `src/preload/index.d.ts`.
</objective>

<execution_context>
@D:/Projects/resumeHelper/.claude/get-shit-done/workflows/execute-plan.md
@D:/Projects/resumeHelper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-margin-controls-live-preview-in-optimize/40-CONTEXT.md
@.planning/phases/40-margin-controls-live-preview-in-optimize/40-PATTERNS.md

<interfaces>
<!-- The effectiveMargins field already exists upstream — this plan only surfaces its TYPE to the renderer. -->

From src/main/lib/mergeHelper.ts (already shipped in Phase 39 — do NOT modify):
```typescript
export type EffectiveMargins = { top: number; bottom: number; sides: number }
export type MergedBuilderData = {
  // ...
  effectiveMargins: EffectiveMargins // always present — resolved at merge time (D-07)
}
```

The IPC handler `templates:getBuilderData` (src/main/handlers/templates.ts:649) returns
`buildMergedBuilderData(...)` directly, so `effectiveMargins` already travels over the wire.
This plan only adds the matching field to the renderer-facing `BuilderData` type so callsites
can read it type-safely.

Current BuilderData interface (src/preload/index.d.ts:155-168), last two fields:
```typescript
  showSummary?: boolean
  summaryOverride?: string
}
```

Analog for the new constants module — src/renderer/src/lib/scoreColor.ts is the only existing
file in src/renderer/src/lib/: a thin, single-concern export module with a JSDoc block.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared MARGIN_FLOOR constant module</name>
  <files>src/renderer/src/lib/marginConstants.ts</files>
  <read_first>
    - src/renderer/src/lib/scoreColor.ts (the only existing file in this lib dir — mirror its thin exported-constant + JSDoc structure)
    - .planning/phases/40-margin-controls-live-preview-in-optimize/40-PATTERNS.md §"src/renderer/src/lib/marginConstants.ts" (the exact module pattern to produce)
  </read_first>
  <action>
    Create a new file `src/renderer/src/lib/marginConstants.ts` exporting a single named constant `MARGIN_FLOOR` with value `0.4` (inches). Mirror the thin single-concern module style of `scoreColor.ts`: a leading JSDoc block explaining that `MARGIN_FLOOR` is the minimum allowed margin in inches, consumed by Phase 40's slider `min` attribute in OptimizeVariant and by Phase 41's auto-fit clamp lower bound, and that keeping it in one place ensures the manual-drag floor and the auto-fit floor can never drift (per CONTEXT D-03). Do not export anything else. Do not import anything.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/renderer/src/lib/marginConstants.ts','utf8');if(!/export const MARGIN_FLOOR\s*=\s*0\.4/.test(s)){console.error('MARGIN_FLOOR not exported as 0.4');process.exit(1)};process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file `src/renderer/src/lib/marginConstants.ts` exists and contains `export const MARGIN_FLOOR = 0.4`
    - Source assertion: the file has a JSDoc comment block naming both Phase 40 (slider min) and Phase 41 (auto-fit clamp) as consumers
    - Source assertion: the module exports exactly one symbol and imports nothing
  </acceptance_criteria>
  <done>The `MARGIN_FLOOR` constant is importable as `import { MARGIN_FLOOR } from '../lib/marginConstants'` and equals 0.4.</done>
</task>

<task type="auto">
  <name>Task 2: Expose effectiveMargins on the renderer-facing BuilderData type</name>
  <files>src/preload/index.d.ts</files>
  <read_first>
    - src/preload/index.d.ts §155-168 (the current BuilderData interface — read it so you add the field in the right place, not a guessed location)
    - src/main/lib/mergeHelper.ts §63, §78-92 (confirm the EffectiveMargins shape { top; bottom; sides } and that MergedBuilderData.effectiveMargins is always present)
    - .planning/phases/40-margin-controls-live-preview-in-optimize/40-PATTERNS.md §"src/preload/index.d.ts" (the exact one-line addition)
  </read_first>
  <action>
    In `src/preload/index.d.ts`, add one optional field to the existing `BuilderData` interface immediately after the `summaryOverride?: string` line: `effectiveMargins?: { top: number; bottom: number; sides: number }`. Make it optional (`?`) so no existing callsite breaks. Do NOT change the `analysisLayout` bridge declaration (already complete from Phase 39 at §648-652). Do NOT modify any main-process or handler code — the value already travels over IPC because `templates:getBuilderData` returns the full `MergedBuilderData` (per D-07 single-source decision). The field shape `{ top; bottom; sides }` mirrors `EffectiveMargins` in mergeHelper.ts.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `BuilderData` in `src/preload/index.d.ts` contains `effectiveMargins?: { top: number; bottom: number; sides: number }`
    - Behavior assertion: the field is optional — `npm run typecheck` passes with no new errors at any existing `BuilderData` callsite (e.g. VariantPreview.tsx)
    - Source assertion: the `analysisLayout` bridge block at §648-652 is unchanged
  </acceptance_criteria>
  <done>The renderer can read `builderData.effectiveMargins?.top/.bottom/.sides` type-safely; `npm run typecheck` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC) | This plan adds no new IPC and no new untrusted input path. It surfaces an existing type only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Information Disclosure | `effectiveMargins` on `BuilderData` | accept | The field carries three numeric margin values (inches) already rendered in the preview; no PII or secret. The data already crosses IPC via `getBuilderData` — adding the type exposes nothing new. |
| T-40-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `npm run typecheck` passes (no new type errors).
- `MARGIN_FLOOR` importable and equal to 0.4.
- `BuilderData.effectiveMargins` present and optional.
</verification>

<success_criteria>
- Shared `MARGIN_FLOOR = 0.4` constant exists in `src/renderer/src/lib/marginConstants.ts`.
- `BuilderData` exposes optional `effectiveMargins: { top; bottom; sides }`.
- Typecheck green; no behavior change (type + constant only).
</success_criteria>

<output>
Create `.planning/phases/40-margin-controls-live-preview-in-optimize/40-01-SUMMARY.md` when done.
</output>
