---
phase: 39-analysis-margin-override-data-layer
plan: 02
type: tdd
wave: 2
depends_on: ["39-01"]
files_modified:
  - src/main/lib/mergeHelper.ts
  - tests/unit/main/lib/resolveEffectiveMargins.test.ts
  - tests/unit/main/lib/mergeHelper.effectiveMargins.test.ts
autonomous: true
requirements: [LAYOUT-03]
must_haves:
  truths:
    - "D-06: pure exported resolveEffectiveMargins returns the analysis override triple when an override exists (unit-testable without DB)"
    - "D-06: resolveEffectiveMargins falls back to variant templateOptions margins when no override exists"
    - "D-06: resolveEffectiveMargins falls back to the template default (DOCX_MARGIN_DEFAULTS) when neither override nor variant margin is present"
    - "D-07: buildMergedBuilderData(db, variantId, analysisId) attaches effectiveMargins to MergedBuilderData, resolved by that precedence (single merge path)"
    - "buildMergedBuilderData with no analysisId returns variant/default margins (no override applied)"
  artifacts:
    - path: "src/main/lib/mergeHelper.ts"
      provides: "Exported pure resolveEffectiveMargins + EffectiveMargins type, and effectiveMargins attached to MergedBuilderData"
      exports: ["resolveEffectiveMargins", "EffectiveMargins"]
      contains: "effectiveMargins"
    - path: "tests/unit/main/lib/resolveEffectiveMargins.test.ts"
      provides: "Pure precedence unit tests (override > variant > default), no DB"
    - path: "tests/unit/main/lib/mergeHelper.effectiveMargins.test.ts"
      provides: "Integration test: buildMergedBuilderData resolves margins from the override row"
  key_links:
    - from: "src/main/lib/mergeHelper.ts buildMergedBuilderData"
      to: "analysis_layout_overrides row"
      via: "db select on analysisLayoutOverrides for analysisId"
      pattern: "analysisLayoutOverrides"
    - from: "src/main/lib/mergeHelper.ts buildMergedBuilderData return"
      to: "MergedBuilderData.effectiveMargins"
      via: "resolveEffectiveMargins(layoutTemplate, variantOptions, overrideRow)"
      pattern: "resolveEffectiveMargins\\("
---

<objective>
Add the pure, exported `resolveEffectiveMargins` resolver (three-tier precedence:
analysis override → variant `templateOptions` → template default), and attach its
result as `effectiveMargins` on `MergedBuilderData` inside `buildMergedBuilderData`.
This collapses margin resolution onto the single merge path so every downstream
surface (Plan 03) reads one resolved triple.

Purpose: SC#2 / LAYOUT-03 — `buildMergedBuilderData(db, variantId, analysisId)`
returns effective margins by the locked precedence (D-06/D-07). The resolver is pure
so the precedence is unit-testable without DB plumbing (SC#5).
Output: `resolveEffectiveMargins` + `EffectiveMargins` exported; `MergedBuilderData`
grows an always-present `effectiveMargins` field; unit + integration tests prove precedence.
</objective>

<execution_context>
@D:/Projects/resumeHelper/.claude/get-shit-done/workflows/execute-plan.md
@D:/Projects/resumeHelper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-analysis-margin-override-data-layer/39-CONTEXT.md
@.planning/phases/39-analysis-margin-override-data-layer/39-PATTERNS.md
@.planning/phases/39-analysis-margin-override-data-layer/39-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (39-01) + existing codebase. -->

From src/main/db/schema.ts (created in 39-01):
  analysisLayoutOverrides — columns { id, analysisId, marginTop, marginBottom, marginSides }.

From src/main/handlers/templates.ts (39-01): getAnalysisMargins(db, analysisId) returns
the override row or null — buildMergedBuilderData may call this or query the table directly.

From src/main/lib/docxBuilder.ts line 34:
  export const DOCX_MARGIN_DEFAULTS: Record<string, { top: number; bottom: number; sides: number }>
  (fallback when key missing: { top: 1.0, bottom: 1.0, sides: 1.0 }).

Existing MergedBuilderData (mergeHelper.ts lines 54-67) already threads showSummary +
summaryOverride resolved-then-attached at the return site — MIRROR that exact pattern.

buildMergedBuilderData signature (mergeHelper.ts lines 69-73): (db, variantId, analysisId?).
Cross-variant guard at lines 172-184 stays unchanged.
</interfaces>
</context>

<feature>
  <name>resolveEffectiveMargins + effectiveMargins on MergedBuilderData</name>
  <files>src/main/lib/mergeHelper.ts, tests/unit/main/lib/resolveEffectiveMargins.test.ts, tests/unit/main/lib/mergeHelper.effectiveMargins.test.ts</files>
  <behavior>
    resolveEffectiveMargins(layoutTemplate, variantOptions, override):
    - override present (all three) → returns override.marginTop/Bottom/Sides regardless of variant/default
    - override null, variant has marginTop only → top=variant.marginTop, bottom/sides=default
    - override null, variantOptions null → returns DOCX_MARGIN_DEFAULTS[layoutTemplate]
    - unknown layoutTemplate, no override, no variant → returns the {1.0,1.0,1.0} fallback
    - per-field precedence is independent (override?.marginX ?? variant?.marginX ?? default.X)
    buildMergedBuilderData(db, variantId, analysisId):
    - with an override row for analysisId → effectiveMargins equals the override triple
    - with no override row but variant templateOptions margins → effectiveMargins equals variant margins
    - with analysisId undefined → no override lookup; effectiveMargins from variant/default
    - effectiveMargins is ALWAYS present on the returned object (not optional)
  </behavior>
  <implementation>
    Place `resolveEffectiveMargins` and `export type EffectiveMargins = { top; bottom; sides }`
    in mergeHelper.ts (planner's call per D-06 discretion — keeps it beside the merge path; if a
    sibling module is preferred, export from src/main/lib/marginResolver.ts and import here). Import
    DOCX_MARGIN_DEFAULTS from '../lib/docxBuilder' (adjust relative path). The function takes
    (layoutTemplate: string, variantOptions: { marginTop?; marginBottom?; marginSides? } | null,
    override: { marginTop; marginBottom; marginSides } | null) and returns the resolved triple using
    `override?.marginX ?? variantOptions?.marginX ?? defaults.X` for each of top/bottom/sides.
    In buildMergedBuilderData: (1) ensure the variant's layoutTemplate + templateOptions are available
    (fetch the variant row if not already in scope); parse templateOptions JSON safely (try/catch → {}).
    (2) When analysisId is provided, fetch the override row from analysisLayoutOverrides (reuse
    getAnalysisMargins or a direct select); null when absent or analysisId undefined. (3) Call
    resolveEffectiveMargins and add `effectiveMargins` to the returned object — mirror exactly how
    showSummary/summaryOverride are resolved-then-returned (lines 395-418). (4) Add
    `effectiveMargins: EffectiveMargins` (NOT optional) to the MergedBuilderData type. Do NOT touch the
    cross-variant guard (lines 172-184). RED→GREEN→REFACTOR: write resolveEffectiveMargins.test.ts
    (pure, no DB) RED first, implement to GREEN; then mergeHelper.effectiveMargins.test.ts using
    createTestDb() + the 39-01 set handler to seed an override.
  </implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED+GREEN resolveEffectiveMargins pure resolver</name>
  <files>src/main/lib/mergeHelper.ts, tests/unit/main/lib/resolveEffectiveMargins.test.ts</files>
  <read_first>
    - src/main/lib/docxBuilder.ts lines 30-45 (DOCX_MARGIN_DEFAULTS shape + fallback)
    - src/main/handlers/export.ts lines 31-40 (existing 2-tier margin fallback chain being generalized)
    - tests/unit/main/lib/mergeHelper.precedence.test.ts (existing pure-test style analog)
    - .planning/phases/39-analysis-margin-override-data-layer/39-PATTERNS.md §"NEW resolveEffectiveMargins"
  </read_first>
  <action>
    Write tests/unit/main/lib/resolveEffectiveMargins.test.ts FIRST (RED): cases for override-wins,
    variant-fallback, default-fallback, unknown-template fallback, and independent per-field precedence.
    Then add `export type EffectiveMargins` and `export function resolveEffectiveMargins(layoutTemplate,
    variantOptions, override)` to mergeHelper.ts using the `?? ` precedence chain over DOCX_MARGIN_DEFAULTS.
    Pure — no DB, no IO.
  </action>
  <verify>
    <automated>npm test -- tests/unit/main/lib/resolveEffectiveMargins.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - resolveEffectiveMargins and EffectiveMargins are exported from mergeHelper.ts
    - A test asserts override > variant > default precedence with each tier covered independently
    - A test asserts unknown layoutTemplate yields the {1.0,1.0,1.0} fallback
    - npm test for resolveEffectiveMargins.test.ts exits 0
  </acceptance_criteria>
  <done>The pure resolver is exported and its precedence is proven by passing unit tests with no DB dependency.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Attach effectiveMargins to MergedBuilderData and resolve at merge time</name>
  <files>src/main/lib/mergeHelper.ts, tests/unit/main/lib/mergeHelper.effectiveMargins.test.ts</files>
  <read_first>
    - src/main/lib/mergeHelper.ts lines 54-67 (MergedBuilderData type), 69-73 (signature), 172-184 (cross-variant guard — leave untouched), 395-418 (showSummary/summaryOverride resolve-then-return site)
    - src/main/handlers/templates.ts getAnalysisMargins (from 39-01) — reuse for the override fetch
    - tests/unit/main/lib/mergeHelper.precedence.test.ts and tests/unit/integration/mergedSurfaces.test.ts (createTestDb seeding analogs)
  </read_first>
  <action>
    Write tests/unit/main/lib/mergeHelper.effectiveMargins.test.ts FIRST (RED) using createTestDb():
    seed a variant (+ its templateOptions margins) and an analysis; assert buildMergedBuilderData returns
    effectiveMargins = variant margins with no override, = override triple after setAnalysisMargins, and
    = variant/default when analysisId is undefined. Then implement: add `effectiveMargins: EffectiveMargins`
    to MergedBuilderData; in buildMergedBuilderData fetch the variant's layoutTemplate + parsed
    templateOptions, fetch the override row only when analysisId is provided, call resolveEffectiveMargins,
    and include effectiveMargins in the returned object — mirroring the showSummary/summaryOverride thread.
    Use bound queries (eq) for the override fetch. Do not alter the cross-variant guard.
  </action>
  <verify>
    <automated>npm test -- tests/unit/main/lib/mergeHelper.effectiveMargins.test.ts 2>&1 | tail -25; npm run typecheck:node 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MergedBuilderData has a non-optional `effectiveMargins: EffectiveMargins` field
    - A test asserts effectiveMargins equals the override triple when an override row exists for analysisId
    - A test asserts effectiveMargins falls back to variant margins when no override exists
    - A test asserts analysisId undefined applies no override
    - npm run typecheck:node passes (every existing MergedBuilderData return site still satisfies the type)
    - npm test for the new file exits 0
  </acceptance_criteria>
  <done>buildMergedBuilderData resolves and returns effectiveMargins by the locked precedence; integration tests prove override/variant/default behavior and typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite → main (override row read) | The override row read in buildMergedBuilderData is internal/trusted data written through Plan 01's validated handler. |
| variant templateOptions JSON parse | Stored JSON string parsed at merge time; malformed JSON must not crash the merge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05 | Tampering / DoS | variant.templateOptions JSON.parse in buildMergedBuilderData | mitigate | Parse inside try/catch defaulting to {} (existing pattern at export.ts:31-40) so a corrupt blob falls back to defaults rather than throwing. |
| T-39-06 | Tampering | override-row fetch SQL | mitigate | Use Drizzle bound `eq(analysisLayoutOverrides.analysisId, analysisId)` — no interpolation. |
| T-39-07 | Information Disclosure | cross-variant analysis leakage | mitigate | Existing cross-variant fail-closed guard (lines 172-184) is preserved unchanged; margin resolution is downstream of it. |
</threat_model>

<verification>
- `npm test -- tests/unit/main/lib/resolveEffectiveMargins.test.ts` exits 0
- `npm test -- tests/unit/main/lib/mergeHelper.effectiveMargins.test.ts` exits 0
- `npm run typecheck:node` passes (effectiveMargins added without breaking existing return sites)
</verification>

<success_criteria>
- resolveEffectiveMargins is pure, exported, and unit-tested for the three-tier precedence (SC#5)
- buildMergedBuilderData returns effectiveMargins resolved as override → variant → default (SC#2 / LAYOUT-03)
- effectiveMargins is always present, ready for Plan 03's surfaces to consume
</success_criteria>

<output>
Create `.planning/phases/39-analysis-margin-override-data-layer/39-02-SUMMARY.md` when done
</output>
