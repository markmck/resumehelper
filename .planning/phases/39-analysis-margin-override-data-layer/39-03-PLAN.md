---
phase: 39-analysis-margin-override-data-layer
plan: 03
type: execute
wave: 3
depends_on: ["39-02"]
files_modified:
  - src/main/handlers/export.ts
  - src/main/handlers/submissions.ts
  - tests/unit/main/handlers/submissions.snapshot.test.ts
  - tests/unit/integration/mergedSurfaces.test.ts
autonomous: true
requirements: [LAYOUT-03, LAYOUT-04]
must_haves:
  truths:
    - "D-07: Live PDF export margins come from MergedBuilderData.effectiveMargins (direct variant-row read routed onto the single merge path)"
    - "D-07: Live DOCX export templateOptions margins come from MergedBuilderData.effectiveMargins"
    - "D-08: Logging a submission from an analysis with a margin override freezes the resolved effective margins into snapshot.templateOptions.margin* (no SubmissionSnapshot schema change)"
    - "D-08: The snapshot PERSISTED to submissions.resumeSnapshot is immutable to later variant-margin edits (SC#3 / LAYOUT-04)"
    - "Snapshot-replay PDF export renders the frozen margins (they reach printToPDF's margin args); the replay code site is unchanged"
    - "Clearing the override makes all live surfaces fall back to the variant's margins (SC#4 / LAYOUT-04)"
  artifacts:
    - path: "src/main/handlers/export.ts"
      provides: "Live-PDF and live-DOCX margin reads routed through effectiveMargins"
      contains: "effectiveMargins"
    - path: "src/main/handlers/submissions.ts"
      provides: "buildSnapshotForVariant freezes effectiveMargins into templateOptions.margin*"
      contains: "effectiveMargins"
    - path: "tests/unit/main/handlers/submissions.snapshot.test.ts"
      provides: "Snapshot-freeze + DB-persisted-immutability coverage"
    - path: "tests/unit/integration/mergedSurfaces.test.ts"
      provides: "Live-surface override/fallback coverage + snapshot-replay margin coverage"
  key_links:
    - from: "src/main/handlers/export.ts live-PDF site"
      to: "MergedBuilderData.effectiveMargins"
      via: "destructure merged.effectiveMargins → printToPDF margin args"
      pattern: "effectiveMargins"
    - from: "src/main/handlers/submissions.ts buildSnapshotForVariant"
      to: "snapshot.templateOptions.margin*"
      via: "freeze effectiveMargins triple into templateOptions"
      pattern: "templateOptions\\.margin(Top|Bottom|Sides)"
    - from: "src/main/handlers/export.ts export:snapshotPdf"
      to: "printToPDF margins"
      via: "snapOpts.marginTop/Bottom → printToPDF margins.top/bottom (frozen replay)"
      pattern: "snapOpts\\?\\.margin(Top|Bottom)"
---

<objective>
Route the three live margin-read sites (live-PDF, live-DOCX, and the snapshot freeze)
onto the single merge path's `effectiveMargins`, and freeze the resolved triple into
`snapshot.templateOptions.margin*` so preview/PDF/DOCX all render the effective margins
and logged submissions become immutable to later variant edits.

Purpose: SC#2 (preview/PDF/DOCX render effective margins, LAYOUT-03), SC#3 (snapshot
freezes resolved margins, LAYOUT-04), SC#4 (clearing falls back across all surfaces).
No submission-snapshot schema change (D-08) — templateOptions.margin* fields already exist.
Output: export.ts + submissions.ts read effectiveMargins; snapshot freeze + DB-persisted
immutability + snapshot-replay margin rendering all proven by tests.
</objective>

<execution_context>
@D:/Projects/resumeHelper/.claude/get-shit-done/workflows/execute-plan.md
@D:/Projects/resumeHelper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-analysis-margin-override-data-layer/39-CONTEXT.md
@.planning/phases/39-analysis-margin-override-data-layer/39-PATTERNS.md
@.planning/phases/39-analysis-margin-override-data-layer/39-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 (39-02) + existing codebase. -->

From src/main/lib/mergeHelper.ts (after 39-02):
  buildMergedBuilderData(db, variantId, analysisId?) → MergedBuilderData with
  effectiveMargins: { top: number; bottom: number; sides: number } (always present).

export.ts sites to change (verified line ranges):
  - live-PDF margin block: lines 31-40 (currently DOCX_MARGIN_DEFAULTS + JSON.parse of variant.templateOptions)
  - live-DOCX templateOptions block: lines 95-114 (already calls buildMergedBuilderData; destructure effectiveMargins and overlay margin* on templateOptions)
  - snapshot-replay PDF block: lines 197-213 — reads snapOpts.marginTop/Bottom, feeds them into win.webContents.printToPDF({ margins: { top, bottom, ... } }) (line 219-223) AND into the postMessage payload (marginSides, line 210). NO CODE CHANGE here (it replays the frozen snapshot, which now holds resolved margins after submissions.ts change). A TEST is added in Task 1 to lock this behavior.

submissions.ts buildSnapshotForVariant: lines 28-64. Already destructures
{ showSummary, summaryOverride, ...builderArrays } from merged and freezes showSummary into
templateOptions — extend the SAME if/else to set marginTop/Bottom/Sides from effectiveMargins.
SubmissionSnapshot.templateOptions.margin* fields already exist (submissions.ts line 14) — no type change.

submissions.ts createSubmission: lines 102-135. Builds the snapshot via buildSnapshotForVariant
then PERSISTS it as `resumeSnapshot: JSON.stringify(snapshot)` into the submissions table (text column,
schema.ts:148). This is the DB round-trip seam Task 2's immutability test uses.

printToPDF wiring is unchanged — only the SOURCE of the margin numbers changes (D-07).
marginSides flows to template CSS via the existing postMessage payload (unchanged split).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route live-PDF and live-DOCX margin reads through effectiveMargins; lock snapshot-replay margins</name>
  <files>src/main/handlers/export.ts, tests/unit/integration/mergedSurfaces.test.ts</files>
  <read_first>
    - src/main/handlers/export.ts lines 31-40 (live-PDF margin chain to replace), 95-114 (live-DOCX templateOptions block), 120-228 (export:snapshotPdf handler — snapshot-replay; confirm NO code change, note printToPDF margins at lines 219-223 and postMessage marginSides at line 210)
    - src/main/lib/mergeHelper.ts effectiveMargins on MergedBuilderData (from 39-02)
    - tests/unit/integration/mergedSurfaces.test.ts (existing surface-integration analog to extend; note it does NOT yet mock electron)
    - .planning/phases/39-analysis-margin-override-data-layer/39-PATTERNS.md §"MODIFY src/main/handlers/export.ts"
  </read_first>
  <behavior>
    - Live-PDF export uses effectiveMargins.top/bottom for the printToPDF margin args
    - Live-DOCX export's templateOptions carries effectiveMargins.top/bottom/sides as marginTop/Bottom/Sides
    - With an analysis override set, both surfaces use the override triple
    - With the override cleared, both surfaces fall back to the variant's margins (SC#4)
    - Snapshot-replay PDF path is unchanged (still reads snapOpts.margin*) AND is now regression-tested:
      a snapshot whose templateOptions.margin* are pre-set to frozen effective values must drive those
      exact values into the printToPDF margin args (top/bottom) and the postMessage payload (marginSides).
  </behavior>
  <action>
    In export.ts live-PDF site (lines 31-40): replace the DOCX_MARGIN_DEFAULTS + JSON.parse fallback
    block with a `buildMergedBuilderData(db, variantId, analysisId)` call (if not already invoked on this
    path) and read `pdfMarginTop = effectiveMargins.top`, `pdfMarginBottom = effectiveMargins.bottom`.
    marginSides continues to reach template CSS via the existing postMessage payload — do not change that
    split or the printToPDF wiring. In the live-DOCX site (lines 95-114): destructure `effectiveMargins`
    from the already-present merged object and overlay `marginTop/marginBottom/marginSides` onto the
    templateOptions object passed to the DOCX builder (parsed variant options spread first, then resolved
    margins overwrite). Leave the snapshot-replay PDF block (export:snapshotPdf, lines 197-213) UNCHANGED
    (D-08 — only a test is added, not a code edit).

    Extend mergedSurfaces.test.ts with two additions:
    (1) LIVE-SURFACE OVERRIDE/FALLBACK — assert that with an analysis margin override set, the merge path's
    effectiveMargins (the source both live surfaces now read) equals the override triple; and with the
    override cleared, effectiveMargins falls back to the variant's margins (SC#2, SC#4). Prefer asserting
    via buildMergedBuilderData (already imported, no electron mock needed) to keep the test light.
    (2) SNAPSHOT-REPLAY MARGIN REGRESSION — exercise the export:snapshotPdf path with a snapshotData whose
    templateOptions.marginTop/marginBottom/marginSides are pre-set to frozen effective values, and assert
    those exact values reach printToPDF's margin args. Mock the `electron` module (BrowserWindow, dialog,
    ipcMain) so the handler runs headless: stub dialog.showSaveDialog to return a filePath; stub
    BrowserWindow to a fake whose webContents.printToPDF is a spy returning a Buffer (and executeJavaScript /
    loadFile / loadURL are no-ops); make ipcMain.once('print:ready', cb) invoke cb immediately so the handler
    does not hang on the 3s timeout; stub fs.writeFile. Capture the args passed to printToPDF and assert
    margins.top === frozen marginTop and margins.bottom === frozen marginBottom; optionally assert the
    executeJavaScript postMessage string contains the frozen marginSides. Register the handler via
    registerExportHandlers and invoke the captured 'export:snapshotPdf' handler directly with the crafted
    snapshotData. Keep this test in mergedSurfaces.test.ts (Task 1's owned file) — do not add a new file.
  </action>
  <verify>
    <automated>npm test -- tests/unit/integration/mergedSurfaces.test.ts 2>&1 | tail -25; npm run typecheck:node 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - export.ts live-PDF margin values are sourced from effectiveMargins (no standalone JSON.parse-of-variant margin chain remains at that site)
    - export.ts live-DOCX templateOptions margin* fields are set from effectiveMargins
    - Snapshot-replay block (export:snapshotPdf, lines 197-213) is unchanged in code
    - A test asserts override-set vs override-cleared yields override vs variant margins on the merge path the live surfaces read (SC#2, SC#4)
    - A test exercises export:snapshotPdf with frozen templateOptions.margin* and asserts those exact values reach printToPDF's margins.top/bottom (and marginSides reaches the postMessage payload) — proving the replay path renders frozen margins (LAYOUT-04 replay guarantee)
    - npm run typecheck:node passes; mergedSurfaces.test.ts exits 0
  </acceptance_criteria>
  <done>Both live surfaces draw margins from the single merge path; override and fallback behavior proven by integration tests; the replay path is untouched in code but its frozen-margin rendering is now regression-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze effectiveMargins into the submission snapshot</name>
  <files>src/main/handlers/submissions.ts, tests/unit/main/handlers/submissions.snapshot.test.ts</files>
  <read_first>
    - src/main/handlers/submissions.ts lines 14 (SubmissionSnapshot.templateOptions shape — margin* already present), 28-64 (buildSnapshotForVariant; the if/else that freezes showSummary), 102-135 (createSubmission — serializes the snapshot via JSON.stringify into submissions.resumeSnapshot)
    - src/main/db/schema.ts (submissions.resumeSnapshot is a `text` column holding the JSON-stringified snapshot, line 148)
    - src/main/lib/mergeHelper.ts effectiveMargins on MergedBuilderData (from 39-02)
    - tests/unit/main/handlers/submissions.snapshot.test.ts (existing snapshot test to extend; uses createTestDb + factories, calls buildSnapshotForVariant directly)
    - tests/helpers/factories.ts (seedAnalysis, seedVariant, updateProfile) and src/main/handlers/templates.ts setAnalysisMargins / clearAnalysisMargins / setVariantOptions (from 39-01 / existing)
    - .planning/phases/39-analysis-margin-override-data-layer/39-PATTERNS.md §"MODIFY buildSnapshotForVariant"
  </read_first>
  <behavior>
    - buildSnapshotForVariant freezes effectiveMargins.top/bottom/sides into templateOptions.marginTop/Bottom/Sides
    - With an analysis override, the snapshot's frozen margins equal the override triple
    - With no override, the snapshot freezes the variant's (resolved) margins
    - DB-PERSISTED IMMUTABILITY (the real SC#3 / LAYOUT-04 guarantee): the snapshot serialized into
      submissions.resumeSnapshot is immutable. Asserting an in-memory plain-object copy is "unchanged"
      after a variant edit proves nothing (buildSnapshotForVariant returns a value copy that no later edit
      could ever touch) — so the test MUST round-trip through the DB:
      (1) seed an analysis with a margin override via setAnalysisMargins (39-01),
      (2) persist a submission for that variant+analysis via createSubmission (writes JSON to submissions.resumeSnapshot),
      (3) mutate the variant's templateOptions margins via setVariantOptions (or clear the override via clearAnalysisMargins),
      (4) re-read the submissions.resumeSnapshot row from the DB and JSON.parse it,
      (5) assert the re-read snapshot.templateOptions.marginTop/marginBottom/marginSides still equal the
          originally-frozen effective (override) margins — NOT the new variant margins.
    - Snapshot-replay PDF export reads these frozen margins unchanged (rendering verified by Task 1's replay test)
  </behavior>
  <action>
    In buildSnapshotForVariant (lines 28-64): destructure `effectiveMargins` from the merged object
    (alongside the existing showSummary/summaryOverride destructure). Extend the existing if/else that
    freezes showSummary so BOTH branches also set `templateOptions.marginTop = effectiveMargins.top`,
    `templateOptions.marginBottom = effectiveMargins.bottom`, `templateOptions.marginSides = effectiveMargins.sides`
    (and the else-branch object literal includes them). Do NOT change the SubmissionSnapshot type — the
    margin* fields already exist (D-08, no snapshot schema change).

    Extend submissions.snapshot.test.ts with two cases:
    (a) FREEZE test — seed a variant + an analysis (seedAnalysis) with a margin override via setAnalysisMargins
    (39-01); call buildSnapshotForVariant(db, variant.id, analysis.id); assert
    snapshot.templateOptions.marginTop/marginBottom/marginSides equal the override triple.
    (b) DB-PERSISTED IMMUTABILITY test — with the same override in place, call createSubmission(db, { company,
    role, variantId: variant.id, analysisId: analysis.id }) so the snapshot is JSON-stringified into
    submissions.resumeSnapshot; capture the returned submission id; then mutate the variant's margins via
    setVariantOptions(db, variant.id, { marginTop: <clearly-different>, marginBottom: <different>,
    marginSides: <different> }) (and/or clearAnalysisMargins(db, analysis.id)); then re-read the persisted row
    (db.select().from(submissions).where(eq(submissions.id, created.id))), JSON.parse its resumeSnapshot, and
    assert the parsed templateOptions.marginTop/marginBottom/marginSides STILL equal the originally-frozen
    override triple (NOT the post-mutation variant values). This proves the persisted snapshot — the artifact
    LAYOUT-04 protects — is immutable. Import { submissions } from the schema and { eq } from drizzle-orm for
    the re-read.
  </action>
  <verify>
    <automated>npm test -- tests/unit/main/handlers/submissions.snapshot.test.ts 2>&1 | tail -25; npm run typecheck:node 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - buildSnapshotForVariant sets templateOptions.marginTop/marginBottom/marginSides from effectiveMargins in BOTH if/else branches
    - No change to the SubmissionSnapshot type (D-08)
    - A test asserts buildSnapshotForVariant freezes the override triple when an override exists
    - A DB-round-trip test asserts the PERSISTED snapshot is immutable: after createSubmission writes the
      snapshot to submissions.resumeSnapshot, mutating the variant's margins (or clearing the override) and
      then re-reading + JSON.parse-ing the resumeSnapshot row yields templateOptions.margin* still equal to
      the originally-frozen override triple, not the new variant margins (SC#3 / LAYOUT-04). The test does NOT
      rely on asserting an in-memory return-value copy is unchanged.
    - npm run typecheck:node passes; submissions.snapshot.test.ts exits 0
  </acceptance_criteria>
  <done>Snapshots freeze the resolved effective margins into the existing templateOptions.margin* shape; the snapshot persisted to submissions.resumeSnapshot stays immutable to later variant edits, proven by a DB round-trip test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main → Chromium printToPDF | Resolved margin numbers feed printToPDF args; numeric, from the validated merge path. |
| snapshot persistence | Frozen templateOptions serialized into the submission snapshot row (submissions.resumeSnapshot, text). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-08 | Tampering / Integrity | snapshot margin freeze | mitigate | Freeze resolved effectiveMargins by value into templateOptions at snapshot-build time so later variant/override edits cannot mutate a logged snapshot (SC#3). Immutability asserted by a DB round-trip test (persist → mutate variant → re-read submissions.resumeSnapshot → frozen margins unchanged). |
| T-39-09 | Tampering | printToPDF margin args | accept | Values are finite numbers validated at write (T-39-01) and resolved through the pure resolver; Chromium clamps physically out-of-range margins. No bounds enforced here per D-09 (owned by Phase 40/41). |
| T-39-10 | Repudiation | which margins were sent | mitigate | The snapshot records the exact frozen margins, preserving the "which version was sent" audit guarantee (project core value). The snapshot-replay PDF path is regression-tested to push those frozen margins into printToPDF, so the rendered audit artifact matches the recorded one. |
</threat_model>

<verification>
- `npm test -- tests/unit/integration/mergedSurfaces.test.ts` exits 0
- `npm test -- tests/unit/main/handlers/submissions.snapshot.test.ts` exits 0
- `npm run typecheck:node` passes
- Full suite green: `npm test` (regression check — no existing surface broken by the rewired margin source)
</verification>

<success_criteria>
- Preview, PDF, and DOCX all render at the resolved effective margins (SC#2 / LAYOUT-03)
- Submission snapshots freeze the analysis-resolved margins and the persisted snapshot stays immutable to later edits; the replay path renders the frozen margins (SC#3 / LAYOUT-04)
- Clearing the override falls back to variant margins across all surfaces (SC#4)
</success_criteria>

<output>
Create `.planning/phases/39-analysis-margin-override-data-layer/39-03-SUMMARY.md` when done
</output>
