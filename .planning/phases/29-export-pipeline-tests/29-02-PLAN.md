---
phase: 29-export-pipeline-tests
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - tests/unit/main/lib/docxBuilder.test.ts
autonomous: true
requirements: [EXPORT-01]
must_haves:
  truths:
    - "Each of the five templates produces DOCX XML containing its correct font name"
    - "Section headings (EXPERIENCE, SKILLS, EDUCATION, PROJECTS) appear in the DOCX XML"
    - "Per-template page margins appear as correct twip values in the DOCX XML"
    - "User templateOptions margin overrides take precedence over template defaults"
    - "Bullet text and profile name appear in the DOCX XML"
  artifacts:
    - path: "tests/unit/main/lib/docxBuilder.test.ts"
      provides: "DOCX builder unit tests with per-template parameterized assertions"
      min_lines: 80
  key_links:
    - from: "tests/unit/main/lib/docxBuilder.test.ts"
      to: "src/main/lib/docxBuilder.ts"
      via: "import { buildResumeDocx, DOCX_FONT_MAP, DOCX_MARGIN_DEFAULTS }"
      pattern: "import.*buildResumeDocx.*from.*docxBuilder"
    - from: "tests/unit/main/lib/docxBuilder.test.ts"
      to: "fflate"
      via: "import { unzipSync } from 'fflate'"
      pattern: "unzipSync"
---

<objective>
Write comprehensive DOCX builder tests that assert against serialized XML output for all five templates, covering font names, section headings, page margins, margin overrides, bullet text, and profile name.

Purpose: EXPORT-01 requires verified DOCX generation with correct paragraph structure, heading styles, and per-template font names. Testing against the XML contract (not in-memory objects) ensures stability across docx library versions.

Output: `tests/unit/main/lib/docxBuilder.test.ts` with parameterized per-template tests.
</objective>

<execution_context>
@D:/Projects/resumeHelper/.claude/get-shit-done/workflows/execute-plan.md
@D:/Projects/resumeHelper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-export-pipeline-tests/29-CONTEXT.md
@.planning/phases/29-export-pipeline-tests/29-RESEARCH.md
@.planning/phases/29-export-pipeline-tests/29-01-SUMMARY.md

<interfaces>
<!-- From src/main/lib/docxBuilder.ts (created in Plan 01) -->
```typescript
export const DOCX_FONT_MAP: Record<string, string> = {
  classic: 'Georgia',
  modern: 'Calibri',
  jake: 'Calibri',
  minimal: 'Calibri',
  executive: 'Garamond',
}

export const DOCX_MARGIN_DEFAULTS: Record<string, { top: number; bottom: number; sides: number }> = {
  classic:   { top: 1.00, bottom: 1.00, sides: 1.00 },
  modern:    { top: 0.75, bottom: 0.75, sides: 0.75 },
  jake:      { top: 0.60, bottom: 0.60, sides: 0.50 },
  minimal:   { top: 1.00, bottom: 1.00, sides: 1.00 },
  executive: { top: 0.80, bottom: 0.80, sides: 0.80 },
}

export interface BuilderData {
  jobs: BuilderJob[]
  skills: BuilderSkill[]
  projects: BuilderProject[]
  education: BuilderEducation[]
  volunteer: BuilderVolunteer[]
  awards: BuilderAward[]
  publications: BuilderPublication[]
  languages: BuilderLanguage[]
  interests: BuilderInterest[]
  references: BuilderReference[]
}

export function buildResumeDocx(
  builderData: BuilderData,
  profileRow: { name?: string | null; email?: string | null; phone?: string | null; location?: string | null; linkedin?: string | null; summary?: string | null } | undefined,
  templateKey: string,
  templateOptions: { marginTop?: number; marginBottom?: number; marginSides?: number; skillsDisplay?: string; accentColor?: string }
): Document
```

<!-- fflate API -->
```typescript
import { unzipSync } from 'fflate'
// unzipSync(data: Uint8Array): { [filename: string]: Uint8Array }
```

<!-- docx Packer API -->
```typescript
import { Packer } from 'docx'
// Packer.toBuffer(doc: Document): Promise<Buffer>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-template font and margin assertions via XML</name>
  <files>tests/unit/main/lib/docxBuilder.test.ts</files>
  <read_first>
    - D:/Projects/resumeHelper/src/main/lib/docxBuilder.ts (the extracted pure function — read FULL to understand exports and BuilderData shape)
    - D:/Projects/resumeHelper/src/preload/index.d.ts (BuilderJob, BuilderSkill type definitions for constructing test data)
  </read_first>
  <action>
Create `tests/unit/main/lib/docxBuilder.test.ts` with the following structure:

**1. Imports:**
```typescript
import { describe, test, expect } from 'vitest'
import { Packer } from 'docx'
import { unzipSync } from 'fflate'
import { buildResumeDocx, DOCX_FONT_MAP, DOCX_MARGIN_DEFAULTS } from '../../../../src/main/lib/docxBuilder'
import type { BuilderData } from '../../../../src/main/lib/docxBuilder'
```

**2. Inline `unzipDocxXml` helper** (per D-08, inline since only this file needs it):
```typescript
async function unzipDocxXml(doc: ReturnType<typeof buildResumeDocx>): Promise<string> {
  const buffer = await Packer.toBuffer(doc)
  const uint8 = new Uint8Array(buffer)
  const files = unzipSync(uint8)
  return new TextDecoder('utf-8').decode(files['word/document.xml'])
}
```

**3. Shared test data — `buildTestBuilderData()` and `buildTestProfile()`:**
```typescript
function buildTestProfile() {
  return {
    name: 'Jane Smith',
    email: 'jane@example.com',
    phone: '555-0199',
    location: 'San Francisco, CA',
    linkedin: 'janesmith',
    summary: null,
  }
}

function buildTestBuilderData(): BuilderData {
  return {
    jobs: [{
      id: 1, company: 'Acme Corp', role: 'Senior Engineer', startDate: '2023-01', endDate: null, excluded: false,
      bullets: [{ id: 1, text: 'Designed scalable microservices architecture', sortOrder: 0, excluded: false }],
    }],
    skills: [{ id: 1, name: 'TypeScript', tags: '[]', categoryId: null, categoryName: 'Languages', excluded: false }],
    projects: [{ id: 1, name: 'OpenSource Tool', sortOrder: 0, excluded: false,
      bullets: [{ id: 1, text: 'Built CLI tool for automation', sortOrder: 0, excluded: false }] }],
    education: [{ id: 1, institution: 'MIT', area: 'Computer Science', studyType: 'BS', startDate: '2018', endDate: '2022', score: '', courses: '[]', excluded: false }],
    volunteer: [],
    awards: [],
    publications: [],
    languages: [],
    interests: [],
    references: [],
  }
}
```
Adjust the literal shape to match whatever `BuilderData` actually expects (read `BuilderJob`, `BuilderSkill`, etc. from `src/preload/index.d.ts` to ensure all required fields are present). The key test values are: company `'Acme Corp'`, role `'Senior Engineer'`, bullet text `'Designed scalable microservices architecture'`, profile name `'Jane Smith'`.

**4. Per-template font test** (D-06, D-07) using `test.each`:
```typescript
const templateKeys = ['classic', 'modern', 'jake', 'minimal', 'executive'] as const

describe('buildResumeDocx', () => {
  const profileRow = buildTestProfile()
  const builderData = buildTestBuilderData()

  test.each(templateKeys)('uses correct font for %s template', async (key) => {
    const doc = buildResumeDocx(builderData, profileRow, key, {})
    const xml = await unzipDocxXml(doc)
    const expectedFont = DOCX_FONT_MAP[key]
    expect(xml).toContain(`w:val="${expectedFont}"`)
  })
```

**5. Per-template default margin test** (D-06) using `test.each`:
```typescript
  test.each(templateKeys)('applies correct default margins for %s template', async (key) => {
    const doc = buildResumeDocx(builderData, profileRow, key, {})
    const xml = await unzipDocxXml(doc)
    const defaults = DOCX_MARGIN_DEFAULTS[key]
    const expectedTop = Math.round(defaults.top * 1440)
    const expectedBottom = Math.round(defaults.bottom * 1440)
    const expectedSides = Math.round(defaults.sides * 1440)
    expect(xml).toContain(`w:top="${expectedTop}"`)
    expect(xml).toContain(`w:bottom="${expectedBottom}"`)
    expect(xml).toContain(`w:left="${expectedSides}"`)
    expect(xml).toContain(`w:right="${expectedSides}"`)
  })
```

**6. Margin override precedence test** (D-06 — templateOptions overrides take precedence):
```typescript
  test('templateOptions margin overrides take precedence over defaults', async () => {
    const overrides = { marginTop: 0.5, marginBottom: 0.5, marginSides: 0.3 }
    const doc = buildResumeDocx(builderData, profileRow, 'classic', overrides)
    const xml = await unzipDocxXml(doc)
    // classic defaults are 1.0/1.0/1.0 — overrides should win
    expect(xml).toContain(`w:top="${Math.round(0.5 * 1440)}"`)   // 720
    expect(xml).toContain(`w:bottom="${Math.round(0.5 * 1440)}"`) // 720
    expect(xml).toContain(`w:left="${Math.round(0.3 * 1440)}"`)   // 432
    expect(xml).toContain(`w:right="${Math.round(0.3 * 1440)}"`)  // 432
    // Should NOT contain classic defaults (1440 for top/bottom/sides)
    // (This check is implicit — if 720 is present, 1440 would only appear if duplicated)
  })
```

**7. Content assertions** (D-06 — section headings, bullet text, profile name):
```typescript
  test('contains profile name and contact info', async () => {
    const doc = buildResumeDocx(builderData, profileRow, 'classic', {})
    const xml = await unzipDocxXml(doc)
    expect(xml).toContain('Jane Smith')
    expect(xml).toContain('jane@example.com')
    expect(xml).toContain('555-0199')
  })

  test('contains section headings', async () => {
    const doc = buildResumeDocx(builderData, profileRow, 'classic', {})
    const xml = await unzipDocxXml(doc)
    expect(xml).toContain('EXPERIENCE')
    expect(xml).toContain('SKILLS')
    expect(xml).toContain('EDUCATION')
    expect(xml).toContain('PROJECTS')
  })

  test('contains job bullet text', async () => {
    const doc = buildResumeDocx(builderData, profileRow, 'classic', {})
    const xml = await unzipDocxXml(doc)
    expect(xml).toContain('Designed scalable microservices architecture')
    expect(xml).toContain('Acme Corp')
    expect(xml).toContain('Senior Engineer')
  })
})
```

Adjust section heading text (e.g., `EXPERIENCE` vs `WORK EXPERIENCE`) based on what the actual builder code produces. Read the extracted `buildResumeDocx` to confirm the exact heading strings.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/lib/docxBuilder.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/main/lib/docxBuilder.test.ts exists
    - File contains `import { buildResumeDocx, DOCX_FONT_MAP, DOCX_MARGIN_DEFAULTS }`
    - File contains `import { unzipSync } from 'fflate'`
    - File contains `test.each` with all five template keys: classic, modern, jake, minimal, executive
    - File contains assertion `w:val="${expectedFont}"` (or equivalent per-template font check)
    - File contains assertions for `w:top=`, `w:bottom=`, `w:left=`, `w:right=` margin values
    - File contains margin override test with values different from defaults (0.5, 0.3)
    - File contains assertions for 'Jane Smith', 'EXPERIENCE' (or actual heading), 'Designed scalable microservices architecture'
    - `npx vitest run tests/unit/main/lib/docxBuilder.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All DOCX builder tests pass: per-template fonts (5 templates), per-template default margins (5 templates), margin override precedence, profile name + contact, section headings, bullet text</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/unit/main/lib/docxBuilder.test.ts` exits 0 with all tests passing
2. Test count includes at least: 5 font tests + 5 margin tests + 1 override test + 3 content tests = 14+ tests
3. `npx vitest run` (full suite) still passes
</verification>

<success_criteria>
- All five templates have verified font name assertions against DOCX XML
- All five templates have verified default margin assertions (twip values = inches * 1440)
- Margin override precedence is tested (templateOptions wins over DOCX_MARGIN_DEFAULTS)
- Profile name, contact info, section headings, and bullet text are verified in XML
- All tests pass with `npx vitest run`
</success_criteria>

<output>
After completion, create `.planning/phases/29-export-pipeline-tests/29-02-SUMMARY.md`
</output>
