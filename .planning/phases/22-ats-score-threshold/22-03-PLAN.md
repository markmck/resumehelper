---
phase: 22-ats-score-threshold
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/renderer/src/components/SubmissionLogForm.tsx
  - src/renderer/src/components/AnalysisResults.tsx
  - src/renderer/src/components/AnalysisList.tsx
autonomous: true
requirements: [ATS-07]

must_haves:
  truths:
    - "Submission form shows 'Below target (72/80)' warning when score < threshold"
    - "Warning is informational only — does not block submission"
    - "AnalysisList and AnalysisResults use shared scoreColor utility with unchanged behavior"
  artifacts:
    - path: "src/renderer/src/components/SubmissionLogForm.tsx"
      provides: "Below-target warning banner on submission form"
      contains: "Below target"
    - path: "src/renderer/src/components/AnalysisResults.tsx"
      provides: "Import from shared scoreColor utility"
      contains: "from.*lib/scoreColor"
    - path: "src/renderer/src/components/AnalysisList.tsx"
      provides: "Import from shared scoreColor utility"
      contains: "from.*lib/scoreColor"
  key_links:
    - from: "src/renderer/src/components/SubmissionLogForm.tsx"
      to: "window.api.templates.getThreshold"
      via: "IPC call to load variant threshold"
      pattern: "getThreshold"
    - from: "src/renderer/src/components/AnalysisResults.tsx"
      to: "src/renderer/src/lib/scoreColor.ts"
      via: "import { getScoreColor } (no threshold arg = fixed bands)"
      pattern: "from.*lib/scoreColor"
---

<objective>
Add soft below-target warning banner to SubmissionLogForm and migrate AnalysisList/AnalysisResults to use the shared scoreColor utility (no behavior change for those two).

Purpose: Complete the threshold feature surface — warn users before submitting below-target applications, and consolidate duplicated score color logic into the shared utility.
Output: SubmissionLogForm with warning banner, AnalysisList and AnalysisResults using shared scoreColor.
</objective>

<execution_context>
@D:/Projects/resumeHelper/.claude/get-shit-done/workflows/execute-plan.md
@D:/Projects/resumeHelper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ats-score-threshold/22-CONTEXT.md
@.planning/phases/22-ats-score-threshold/22-RESEARCH.md
@.planning/phases/22-ats-score-threshold/22-UI-SPEC.md
@.planning/phases/22-ats-score-threshold/22-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: shared scoreColor utility -->
// src/renderer/src/lib/scoreColor.ts
export function getScoreColor(score: number, threshold?: number): string
export function getScoreBg(score: number, threshold?: number): string

<!-- From Plan 01: preload API -->
// window.api.templates
getThreshold: (id: number) => Promise<number>

<!-- Existing SubmissionLogForm props (line 8-18) -->
interface Props {
  linkedAnalysisId?: number
  onSaved: () => void
  onBack: () => void
}

<!-- Existing LinkedAnalysis type (used in SubmissionLogForm state) -->
interface LinkedAnalysis {
  id: number
  company: string
  role: string
  score: number
  variantId: number
  variantName: string
  createdAt: string
}

<!-- Existing SubmissionLogForm linkedAnalysis loading (line 92-104) -->
useEffect(() => {
  if (linkedAnalysisId != null) {
    window.api.submissions.getAnalysisById(linkedAnalysisId).then((data) => {
      if (data) setLinkedAnalysis(data)
    })
  }
}, [linkedAnalysisId])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add below-target warning to SubmissionLogForm and migrate color imports</name>
  <files>src/renderer/src/components/SubmissionLogForm.tsx, src/renderer/src/components/AnalysisResults.tsx, src/renderer/src/components/AnalysisList.tsx</files>
  <read_first>
    - src/renderer/src/components/SubmissionLogForm.tsx (full file — especially: imports, Props interface, LinkedAnalysis type, linkedAnalysis loading useEffect around line 92-104, getScoreColor local function around line 33, the linkedAnalysis info card JSX around line 186-210)
    - src/renderer/src/components/AnalysisResults.tsx (getScoreColor and getScoreBg local functions — note exact function names and signatures; also note any getScoreBg variant name like getScoreBgColor)
    - src/renderer/src/components/AnalysisList.tsx (getScoreColor and getScoreBgColor local functions — note exact names)
    - src/renderer/src/lib/scoreColor.ts (created in Plan 01 — confirm export names: getScoreColor, getScoreBg)
    - .planning/phases/22-ats-score-threshold/22-UI-SPEC.md (Component 5: Submission Warning Banner spec)
  </read_first>
  <action>
**SubmissionLogForm.tsx changes:**

1. **Replace local getScoreColor with import:**

Remove the local `getScoreColor` function (around line 33). Add import at top:
```typescript
import { getScoreColor } from '../lib/scoreColor'
```

2. **Add threshold state and load alongside linkedAnalysis:**

Add state:
```typescript
const [threshold, setThreshold] = useState<number | null>(null)
```

In the existing `useEffect` that loads `linkedAnalysis` (around line 92-104), after `setLinkedAnalysis(data)`, add threshold loading. Guard with `data.variantId != null` (Pitfall 5):
```typescript
if (linkedAnalysisId != null) {
  window.api.submissions.getAnalysisById(linkedAnalysisId).then((data) => {
    if (data) {
      setLinkedAnalysis(data)
      if (data.variantId != null) {
        window.api.templates.getThreshold(data.variantId).then((t) => setThreshold(t))
      }
    }
  })
}
```

3. **Add warning banner JSX (per D-18, D-19, UI-SPEC Component 5):**

After the linkedAnalysis info card (around line 210, after the closing `</div>` of the analysis info section) and before the form fields, add:

```tsx
{/* Below-target warning */}
{linkedAnalysis && threshold != null && linkedAnalysis.score < threshold && (
  <div style={{
    background: 'var(--color-warning-bg)',
    border: '1px solid rgba(245,158,11,0.25)',
    borderRadius: 'var(--radius-md)',
    padding: '12px 16px',
    marginBottom: 'var(--space-4)',
  }}>
    <p style={{
      fontSize: 'var(--font-size-base)',
      fontWeight: 600,
      color: 'var(--color-warning)',
      margin: 0,
    }}>
      Below target ({linkedAnalysis.score}/{threshold})
    </p>
    <p style={{
      fontSize: 'var(--font-size-base)',
      fontWeight: 400,
      color: 'var(--color-text-secondary)',
      margin: '4px 0 0 0',
    }}>
      This variant&apos;s match score is below your target. You can still submit.
    </p>
  </div>
)}
```

Per D-19: This is informational only. No blocking dialog. The submit button remains enabled.

Per Pitfall 4: Uses `linkedAnalysis.score` (stored DB matchScore), NOT `computedScore`. This is intentional — SubmissionLogForm does not have access to OptimizeVariant's live acceptance state.

**AnalysisResults.tsx changes:**

1. Remove the local `getScoreColor` function and any local `getScoreBg`/`getScoreBgColor` function.
2. Add import: `import { getScoreColor, getScoreBg } from '../lib/scoreColor'`
3. If the local function was named `getScoreBg` differently (e.g., `getScoreBgColor`), either:
   - Rename the import: `import { getScoreBg as getScoreBgColor } from '../lib/scoreColor'` — OR
   - Update all call sites from `getScoreBgColor(...)` to `getScoreBg(...)`
4. Do NOT pass a threshold argument to any call — per D-10/D-11, AnalysisResults uses fixed 80/50 bands.

**AnalysisList.tsx changes:**

1. Remove the local `getScoreColor` and `getScoreBgColor` functions.
2. Add import: `import { getScoreColor, getScoreBg } from '../lib/scoreColor'`
3. Same rename handling as AnalysisResults if local function name differs from shared export name.
4. Do NOT pass a threshold argument — per D-10, AnalysisList uses fixed 80/50 bands.
  </action>
  <verify>
    <automated>grep -c "Below target" src/renderer/src/components/SubmissionLogForm.tsx && grep -c "from.*lib/scoreColor" src/renderer/src/components/SubmissionLogForm.tsx && grep -c "from.*lib/scoreColor" src/renderer/src/components/AnalysisResults.tsx && grep -c "from.*lib/scoreColor" src/renderer/src/components/AnalysisList.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SubmissionLogForm.tsx imports `getScoreColor` from `'../lib/scoreColor'` (NOT a local function)
    - SubmissionLogForm.tsx local `getScoreColor` function definition is REMOVED
    - SubmissionLogForm.tsx has `const [threshold, setThreshold] = useState<number | null>(null)`
    - SubmissionLogForm.tsx useEffect calls `window.api.templates.getThreshold(data.variantId)` guarded by `data.variantId != null`
    - SubmissionLogForm.tsx contains warning banner with text "Below target ({linkedAnalysis.score}/{threshold})"
    - SubmissionLogForm.tsx warning banner has second line "This variant's match score is below your target. You can still submit."
    - SubmissionLogForm.tsx warning background is `var(--color-warning-bg)`
    - SubmissionLogForm.tsx warning heading color is `var(--color-warning)`
    - SubmissionLogForm.tsx submit button is NOT conditionally disabled by threshold — no `disabled` prop references threshold
    - SubmissionLogForm.tsx uses `linkedAnalysis.score` for comparison (NOT `computedScore`)
    - AnalysisResults.tsx imports from `'../lib/scoreColor'` and has NO local getScoreColor/getScoreBg function
    - AnalysisResults.tsx does NOT pass a second argument (threshold) to getScoreColor or getScoreBg
    - AnalysisList.tsx imports from `'../lib/scoreColor'` and has NO local getScoreColor/getScoreBgColor function
    - AnalysisList.tsx does NOT pass a second argument (threshold) to getScoreColor or getScoreBg
  </acceptance_criteria>
  <done>SubmissionLogForm shows informational "Below target (72/80)" warning when score < threshold, AnalysisList and AnalysisResults use shared color utility with unchanged fixed-band behavior</done>
</task>

</tasks>

<verification>
- `grep -c "Below target" src/renderer/src/components/SubmissionLogForm.tsx` returns >= 1
- `grep -c "getThreshold" src/renderer/src/components/SubmissionLogForm.tsx` returns >= 1
- `grep "function getScoreColor" src/renderer/src/components/SubmissionLogForm.tsx` returns empty (local removed)
- `grep "function getScoreColor" src/renderer/src/components/AnalysisResults.tsx` returns empty (local removed)
- `grep "function getScoreColor" src/renderer/src/components/AnalysisList.tsx` returns empty (local removed)
- `grep -c "from.*lib/scoreColor" src/renderer/src/components/AnalysisResults.tsx` returns >= 1
- `grep -c "from.*lib/scoreColor" src/renderer/src/components/AnalysisList.tsx` returns >= 1
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- SubmissionLogForm displays "Below target (score/threshold)" warning when linkedAnalysis.score < variant threshold
- Warning is non-blocking — submit button works regardless
- Warning uses linkedAnalysis.score (DB matchScore), not live computedScore
- AnalysisList and AnalysisResults use shared scoreColor utility from lib/scoreColor.ts
- AnalysisList and AnalysisResults behavior is unchanged (fixed 80/50 bands, no threshold argument)
- All three files compile without type errors
</success_criteria>

<output>
After completion, create `.planning/phases/22-ats-score-threshold/22-03-SUMMARY.md`
</output>
