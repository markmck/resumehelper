---
phase: 06-projects-in-export-pipeline-and-resume-json-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/preload/index.d.ts
  - src/main/handlers/templates.ts
  - src/main/handlers/export.ts
  - src/main/handlers/submissions.ts
  - src/renderer/src/components/VariantBuilder.tsx
  - src/renderer/src/components/ProfessionalLayout.tsx
  - src/renderer/src/components/VariantPreview.tsx
  - src/renderer/src/components/SnapshotViewer.tsx
  - src/renderer/src/PrintApp.tsx
autonomous: true
requirements:
  - PROJ-03
  - PROJ-04

must_haves:
  truths:
    - "Projects appear as checkboxes in VariantBuilder after Skills section, with project-level + bullet-level toggles"
    - "Toggling a project off in the builder disables all its bullets (cascade behavior)"
    - "Projects section appears on resume preview between Skills and any future sections"
    - "Projects appear in DOCX export with bold project name header and bulleted highlights"
    - "Submission snapshots capture project inclusion/exclusion state"
  artifacts:
    - path: "src/preload/index.d.ts"
      provides: "BuilderProject type, extended BuilderData and SubmissionSnapshot interfaces"
      contains: "BuilderProject"
    - path: "src/main/handlers/templates.ts"
      provides: "Projects in getBuilderData and setItemExcluded handlers"
      contains: "projectBullet"
    - path: "src/main/handlers/export.ts"
      provides: "Projects in getBuilderDataForVariant and DOCX builder"
      contains: "PROJECTS"
    - path: "src/renderer/src/components/ProfessionalLayout.tsx"
      provides: "Projects section rendering on resume"
      contains: "Projects"
  key_links:
    - from: "src/main/handlers/templates.ts"
      to: "templateVariantItems"
      via: "project/projectBullet itemType branches in setItemExcluded"
      pattern: "itemType === 'project'"
    - from: "src/renderer/src/components/VariantBuilder.tsx"
      to: "window.api.templates.setItemExcluded"
      via: "handleProjectToggle and handleProjectBulletToggle"
      pattern: "setItemExcluded.*project"
    - from: "src/renderer/src/components/ProfessionalLayout.tsx"
      to: "BuilderProject"
      via: "projects prop filtering excluded items"
      pattern: "includedProjects"
---

<objective>
Wire projects into the template variant builder, resume preview, PDF/DOCX export, and submission snapshots.

Purpose: Projects were added to the DB in Phase 5 but are not yet usable in resume variants. This plan makes projects first-class citizens in the entire export pipeline — matching the existing jobs pattern exactly.

Output: Projects appear in VariantBuilder with checkbox toggles, render on ProfessionalLayout, export to DOCX, and freeze into submission snapshots.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-projects-in-export-pipeline-and-resume-json-import/06-RESEARCH.md

@src/preload/index.d.ts
@src/main/handlers/templates.ts
@src/main/handlers/export.ts
@src/main/handlers/submissions.ts
@src/main/db/schema.ts
@src/renderer/src/components/VariantBuilder.tsx
@src/renderer/src/components/ProfessionalLayout.tsx
@src/renderer/src/components/VariantPreview.tsx
@src/renderer/src/components/SnapshotViewer.tsx
@src/renderer/src/PrintApp.tsx

<interfaces>
<!-- Existing types the executor needs -->

From src/preload/index.d.ts (current BuilderData — will be extended):
```typescript
export interface BuilderBullet { id: number; text: string; sortOrder: number; excluded: boolean }
export interface BuilderJob { id: number; company: string; role: string; startDate: string; endDate: string | null; excluded: boolean; bullets: BuilderBullet[] }
export interface BuilderSkill { id: number; name: string; tags: string[]; excluded: boolean }
export interface BuilderData { jobs: BuilderJob[]; skills: BuilderSkill[] }
export interface SubmissionSnapshot { layoutTemplate: string; jobs: BuilderJob[]; skills: BuilderSkill[] }
```

From src/main/db/schema.ts (project FK columns already exist on templateVariantItems):
```typescript
export const templateVariantItems = sqliteTable('template_variant_items', {
  // ...existing columns...
  projectId: integer('project_id').references(() => projects.id, { onDelete: 'cascade' }),
  projectBulletId: integer('project_bullet_id').references(() => projectBullets.id, { onDelete: 'cascade' }),
})
```

From src/main/db/schema.ts (project tables):
```typescript
export const projects = sqliteTable('projects', { id, name, sortOrder })
export const projectBullets = sqliteTable('project_bullets', { id, projectId, text, sortOrder })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BuilderProject type and extend backend handlers for project exclusions</name>
  <files>src/preload/index.d.ts, src/main/handlers/templates.ts, src/main/handlers/export.ts, src/main/handlers/submissions.ts</files>
  <action>
1. In `src/preload/index.d.ts`:
   - Add `BuilderProject` interface: `{ id: number; name: string; excluded: boolean; bullets: BuilderBullet[] }` (mirrors BuilderJob but without company/role/dates)
   - Extend `BuilderData` to add `projects: BuilderProject[]`
   - Extend `SubmissionSnapshot` to add `projects: BuilderProject[]`

2. In `src/main/handlers/templates.ts`:
   - Add `import { projects, projectBullets }` from schema
   - In `templates:getBuilderData` handler:
     - Fetch `allProjects` from projects table ordered by `asc(projects.sortOrder)`
     - Fetch `allProjectBullets` from projectBullets ordered by `asc(projectBullets.sortOrder)`
     - Add `excludedProjectIds` and `excludedProjectBulletIds` Sets from exclusionItems where `itemType === 'project'` / `'projectBullet'`
     - Build `projectBulletsByProjectId` Map (same pattern as bulletsByJobId)
     - Build `projectsWithBullets` array with excluded flags
     - Return `{ jobs, skills, projects: projectsWithBullets }`
   - In `templates:setItemExcluded` handler, add two new branches:
     - `itemType === 'project'`: delete/insert exclusion row with `projectId`, then cascade to all project bullets (mirror the job->bullet cascade pattern exactly)
     - `itemType === 'projectBullet'`: delete/insert exclusion row with `projectBulletId` (mirror the bullet branch)
   - In `templates:duplicate` handler: copy `projectId` and `projectBulletId` fields when duplicating sourceItems

3. In `src/main/handlers/export.ts`:
   - Add `import { projects, projectBullets }` from schema
   - Add `import { BuilderProject }` (or define inline) alongside existing BuilderJob/BuilderSkill imports
   - Update local `BuilderData` interface to include `projects: BuilderProject[]`
   - In `getBuilderDataForVariant`: add the same project fetching logic as templates.ts (fetch allProjects, allProjectBullets, build excluded sets, build projectsWithBullets, return in BuilderData)
   - In `export:docx` handler: after the SKILLS section, add a PROJECTS section using the same pattern — section heading paragraph ("PROJECTS"), then for each included project: bold project name paragraph, bulleted highlights below. Use same Paragraph/TextRun/bullet patterns as jobs.

4. In `src/main/handlers/submissions.ts`:
   - Add `import { projects, projectBullets }` from schema
   - Update local `SubmissionSnapshot` interface to include `projects: BuilderProject[]`
   - In `buildSnapshotForVariant`: add the same project fetching + exclusion logic, return projects in snapshot
  </action>
  <verify>
    <automated>cd D:/Projects/resumeHelper && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>BuilderData and SubmissionSnapshot include projects. Both copies of getBuilderDataForVariant return projects with exclusion flags. setItemExcluded handles project and projectBullet itemTypes with cascade. DOCX export renders a PROJECTS section. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add projects to VariantBuilder UI, ProfessionalLayout, PrintApp, VariantPreview, and SnapshotViewer</name>
  <files>src/renderer/src/components/VariantBuilder.tsx, src/renderer/src/components/ProfessionalLayout.tsx, src/renderer/src/components/VariantPreview.tsx, src/renderer/src/components/SnapshotViewer.tsx, src/renderer/src/PrintApp.tsx</files>
  <action>
1. In `src/renderer/src/components/VariantBuilder.tsx`:
   - Import `BuilderProject` from index.d
   - Add `handleProjectToggle(project: BuilderProject)` — optimistic update on `builderData.projects`, call `setItemExcluded(variantId, 'project', project.id, newExcluded)`. Mirror `handleJobToggle` exactly.
   - Add `handleProjectBulletToggle(projectId, bulletId, currentExcluded)` — optimistic update on nested bullet, call `setItemExcluded(variantId, 'projectBullet', bulletId, newExcluded)`. Mirror `handleBulletToggle` exactly.
   - After the Skills section, add a Projects section with heading "Projects" (uppercase tracking-wider like other headings). Render each project as a card with project-level checkbox (name displayed), nested bullet checkboxes below. When project is excluded, bullets are disabled (same `cursor-not-allowed opacity-40` pattern as jobs). Use identical markup/class patterns as the Work History section.

2. In `src/renderer/src/components/ProfessionalLayout.tsx`:
   - Add `BuilderProject` to imports from index.d
   - Add `projects?: BuilderProject[]` to `ProfessionalLayoutProps` (optional for backward compatibility with old snapshots)
   - Compute `includedProjects = (projects ?? []).filter(p => !p.excluded)`
   - After the Skills section, render Projects section:
     - `<h2 style={sectionHeadingStyle}>Projects</h2>` (per user decision: section heading "PROJECTS")
     - For each project: bold name as header (12px, bold, #1a1a1a), then `<ul>` with non-excluded bullets as `<li>` items. Use same styling as job entries but without dates/company line.
   - Update the "no items" empty state check to also consider projects

3. In `src/renderer/src/components/VariantPreview.tsx`:
   - Pass `projects={builderData.projects}` to ProfessionalLayout (it comes from BuilderData automatically)

4. In `src/renderer/src/components/SnapshotViewer.tsx`:
   - Pass `projects={snapshot.projects}` to ProfessionalLayout (handle undefined for old snapshots: `projects={snapshot.projects ?? []}`)

5. In `src/renderer/src/PrintApp.tsx`:
   - Update PrintData interface to include `projects` from BuilderData
   - In useEffect data fetch, include `builderData.projects` in the setData call
   - Pass `projects={data.projects}` to ProfessionalLayout
  </action>
  <verify>
    <automated>cd D:/Projects/resumeHelper && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Projects section appears in VariantBuilder with project-level + bullet-level checkbox toggles positioned after Skills. ProfessionalLayout renders included projects with bold name and bulleted highlights. PrintApp, VariantPreview, and SnapshotViewer all pass projects through to ProfessionalLayout. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npm run build` succeeds
3. Manual smoke test: Create a template variant, verify projects appear with checkboxes in Builder tab. Toggle a project off, verify its bullets become disabled. Switch to Preview tab, verify projects render after Skills. Export PDF and DOCX, verify projects section appears.
</verification>

<success_criteria>
- Projects appear in VariantBuilder with project-level and bullet-level checkboxes after Skills section
- Toggling a project off cascades to disable all its bullet checkboxes
- Projects render on ProfessionalLayout between Skills and the bottom of the resume
- DOCX export includes a PROJECTS section with bold names and bulleted highlights
- Submission snapshots include project data for historical accuracy
- All existing functionality (jobs, skills) continues working unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/06-projects-in-export-pipeline-and-resume-json-import/06-01-SUMMARY.md`
</output>
