---
phase: 06-projects-in-export-pipeline-and-resume-json-import
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/preload/index.d.ts
  - src/main/handlers/templates.ts
  - src/main/handlers/export.ts
  - src/main/handlers/submissions.ts
  - src/renderer/src/components/VariantBuilder.tsx
  - src/renderer/src/components/ProfessionalLayout.tsx
  - src/renderer/src/components/VariantPreview.tsx
  - src/renderer/src/components/SnapshotViewer.tsx
  - src/renderer/src/PrintApp.tsx
autonomous: true
requirements:
  - PROJ-04
  - IMP-01

must_haves:
  truths:
    - "All 7 new entity types appear as toggleable sections in VariantBuilder"
    - "Included new entities render in ProfessionalLayout resume preview"
    - "Included new entities render in DOCX export"
    - "Submission snapshots capture new entity inclusion/exclusion state"
  artifacts:
    - path: "src/preload/index.d.ts"
      provides: "Builder* types for all 7 new entities, extended BuilderData and SubmissionSnapshot"
      contains: "BuilderEducation"
    - path: "src/main/handlers/templates.ts"
      provides: "getBuilderData fetches + excludes all new entities; setItemExcluded handles all new entity types"
      contains: "education"
    - path: "src/main/handlers/export.ts"
      provides: "DOCX sections for all new entity types"
      contains: "EDUCATION"
    - path: "src/renderer/src/components/VariantBuilder.tsx"
      provides: "Checkbox sections for all 7 new entity types"
      contains: "education"
  key_links:
    - from: "src/main/handlers/templates.ts"
      to: "templateVariantItems"
      via: "setItemExcluded branches for education, volunteer, award, etc."
      pattern: "itemType === 'education'"
    - from: "src/renderer/src/components/ProfessionalLayout.tsx"
      to: "BuilderData"
      via: "props for all new entity arrays, filtered by excluded flag"
      pattern: "includedEducation"
---

<objective>
Wire all 7 new resume.json entity types into the template variant builder, resume preview, DOCX export, and submission snapshots — extending the work done for projects in Plan 01.

Purpose: Education, volunteer experience, awards, publications, languages, interests, and references must be toggleable in the builder and rendered in all export formats for complete resume customization.

Output: All new entities appear in VariantBuilder with item-level checkboxes, render in ProfessionalLayout, export to DOCX, and freeze into submission snapshots.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-projects-in-export-pipeline-and-resume-json-import/06-RESEARCH.md
@.planning/phases/06-projects-in-export-pipeline-and-resume-json-import/06-01-SUMMARY.md
@.planning/phases/06-projects-in-export-pipeline-and-resume-json-import/06-02-SUMMARY.md

@src/preload/index.d.ts
@src/main/handlers/templates.ts
@src/main/handlers/export.ts
@src/main/handlers/submissions.ts
@src/main/db/schema.ts
@src/renderer/src/components/VariantBuilder.tsx
@src/renderer/src/components/ProfessionalLayout.tsx
@src/renderer/src/components/VariantPreview.tsx
@src/renderer/src/components/SnapshotViewer.tsx
@src/renderer/src/PrintApp.tsx

<interfaces>
<!-- After Plan 01, BuilderData already has projects. After Plan 02, schema has all new tables. -->
<!-- Executor: read current state of index.d.ts and templates.ts to see Plan 01's additions, then extend further. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Builder types for 7 new entities and extend backend handlers</name>
  <files>src/preload/index.d.ts, src/main/handlers/templates.ts, src/main/handlers/export.ts, src/main/handlers/submissions.ts</files>
  <action>
1. In `src/preload/index.d.ts`, add Builder interfaces for each new entity type:
   - `BuilderEducation { id: number; institution: string; area: string; studyType: string; startDate: string; endDate: string | null; score: string; courses: string[]; excluded: boolean }`
   - `BuilderVolunteer { id: number; organization: string; position: string; startDate: string; endDate: string | null; summary: string; highlights: string[]; excluded: boolean }`
   - `BuilderAward { id: number; title: string; date: string | null; awarder: string; summary: string; excluded: boolean }`
   - `BuilderPublication { id: number; name: string; publisher: string; releaseDate: string | null; url: string; summary: string; excluded: boolean }`
   - `BuilderLanguage { id: number; language: string; fluency: string; excluded: boolean }`
   - `BuilderInterest { id: number; name: string; keywords: string[]; excluded: boolean }`
   - `BuilderReference { id: number; name: string; reference: string; excluded: boolean }`

   Extend `BuilderData` with all 7 as optional arrays (optional for backward compat with existing code until wired):
   ```typescript
   education?: BuilderEducation[]
   volunteer?: BuilderVolunteer[]
   awards?: BuilderAward[]
   publications?: BuilderPublication[]
   languages?: BuilderLanguage[]
   interests?: BuilderInterest[]
   references?: BuilderReference[]
   ```
   Similarly extend `SubmissionSnapshot`.

2. In `src/main/handlers/templates.ts`:
   - Import all 7 new table definitions from schema
   - In `templates:getBuilderData`: fetch all rows from each new table. For each, query exclusionItems for matching `itemType` and build excluded set. Map rows to Builder types (parse JSON fields like courses, highlights, keywords). Return all in BuilderData.
   - In `templates:setItemExcluded`: add 7 new branches for `itemType === 'education'|'volunteer'|'award'|'publication'|'language'|'interest'|'reference'`. Each follows the simple pattern (no cascade needed — sub-items are NOT individually toggleable per locked decisions): delete existing exclusion row, insert new one if excluded=true. Use the appropriate FK column (`educationId`, `volunteerId`, `awardId`, etc.).
   - In `templates:duplicate`: copy all new FK columns when duplicating sourceItems.

3. In `src/main/handlers/export.ts`:
   - Import all 7 new tables from schema
   - Update local `BuilderData` interface to include all 7 new entity arrays
   - In `getBuilderDataForVariant`: add the same fetching + exclusion logic for all 7 entities
   - In `export:docx`: after PROJECTS section, add sections for each non-empty entity type. Recommended order: EDUCATION, VOLUNTEER EXPERIENCE, AWARDS, PUBLICATIONS, LANGUAGES, INTERESTS, REFERENCES. Each uses a section heading Paragraph + entity-specific content:
     - EDUCATION: Each entry as "StudyType in Area — Institution" with dates right-aligned, score below if present
     - VOLUNTEER: Each entry as "Position — Organization" with dates, summary below
     - AWARDS: Each entry as "Title — Awarder (Date)", summary below
     - PUBLICATIONS: Each entry as "Name — Publisher (Date)", URL below
     - LANGUAGES: Comma-separated list "Language (Fluency)" all on one line
     - INTERESTS: Each as "Name: keyword1, keyword2, ..."
     - REFERENCES: Each as "Name" with reference text below
   - Use same Paragraph/TextRun/bullet styling patterns as existing sections

4. In `src/main/handlers/submissions.ts`:
   - Import all 7 new tables
   - Extend `SubmissionSnapshot` and `buildSnapshotForVariant` with the same fetching + exclusion logic for all 7 entities
  </action>
  <verify>
    <automated>cd D:/Projects/resumeHelper && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>BuilderData and SubmissionSnapshot include all 7 new entity types. All three copies of builder data fetching (templates.ts, export.ts, submissions.ts) return new entities with exclusion flags. setItemExcluded handles all new entity types. DOCX export renders all new sections. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add new entity sections to VariantBuilder, ProfessionalLayout, and pass-through components</name>
  <files>src/renderer/src/components/VariantBuilder.tsx, src/renderer/src/components/ProfessionalLayout.tsx, src/renderer/src/components/VariantPreview.tsx, src/renderer/src/components/SnapshotViewer.tsx, src/renderer/src/PrintApp.tsx</files>
  <action>
1. In `src/renderer/src/components/VariantBuilder.tsx`:
   - Import all Builder entity types from index.d
   - Add generic toggle handler for simple entities (no cascade needed):
     ```typescript
     const handleEntityToggle = async (entityType: string, id: number, currentExcluded: boolean) => {
       const newExcluded = !currentExcluded
       // optimistic update on the appropriate array in builderData
       await window.api.templates.setItemExcluded(variantId, entityType, id, newExcluded)
     }
     ```
     Implement the optimistic update for each entity array (use a helper or repeat per entity).
   - After the Projects section (already added in Plan 01), add sections for each new entity type. Builder section order per locked decision: Work, Skills, Projects, Education, then remaining minor sections. Use Claude's discretion for minor section order (recommended: Volunteer, Awards, Publications, Languages, Interests, References).
   - Each section follows the same pattern as Skills: section heading, then list of items with checkboxes.
     - Education: checkbox + "Institution — Area (StudyType)"
     - Volunteer: checkbox + "Organization — Position"
     - Awards: checkbox + "Title — Awarder"
     - Publications: checkbox + "Name — Publisher"
     - Languages: checkbox + "Language (Fluency)"
     - Interests: checkbox + "Name"
     - References: checkbox + "Name"
   - Sub-items (courses, highlights, keywords) are NOT shown as individual checkboxes — parent-level only (per locked decisions)

2. In `src/renderer/src/components/ProfessionalLayout.tsx`:
   - Add optional props for all 7 new entity arrays (using Builder types)
   - After the Projects section (already added in Plan 01), render each non-empty entity section:
     - Same `sectionHeadingStyle` for section headers
     - Section order: Work Experience, Skills, Projects, Education, Volunteer Experience, Awards, Publications, Languages, Interests, References
     - EDUCATION: Each entry rendered with institution/area/studyType/dates. Score shown if present. Courses shown as comma-separated list.
     - VOLUNTEER: Similar to jobs — organization as company, position as role, dates, highlights as bullets
     - AWARDS: Title bold, awarder and date, summary below
     - PUBLICATIONS: Name bold, publisher and date, URL as text, summary below
     - LANGUAGES: Rendered as a single block "Language (Fluency)" comma-separated — compact format
     - INTERESTS: Each interest name bold, keywords comma-separated after
     - REFERENCES: Name bold, reference text below
   - Update empty state check to also consider new entity arrays

3. In `src/renderer/src/components/VariantPreview.tsx`:
   - Pass all new entity arrays from builderData to ProfessionalLayout (e.g., `education={builderData.education}`)

4. In `src/renderer/src/components/SnapshotViewer.tsx`:
   - Pass all new entity arrays from snapshot to ProfessionalLayout (with `?? []` fallback for old snapshots)

5. In `src/renderer/src/PrintApp.tsx`:
   - Extend PrintData interface with all new entity arrays
   - Pass them from builderData through to ProfessionalLayout
  </action>
  <verify>
    <automated>cd D:/Projects/resumeHelper && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 7 new entity types appear as toggleable sections in VariantBuilder. ProfessionalLayout renders all included entities in correct section order. PrintApp, VariantPreview, and SnapshotViewer pass all entity data through. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npm run build` succeeds
3. Manual smoke test: Add an education entry in Experience tab. Open a template variant, verify Education section appears in Builder with checkbox. Toggle it off. Switch to Preview tab — education should not appear. Toggle it back on — education should appear on the resume. Export DOCX — verify Education section renders.
</verification>

<success_criteria>
- All 7 new entity types appear as toggleable sections in VariantBuilder after Projects
- Item-level checkboxes work for each entity (toggle on/off, persists across reload)
- ProfessionalLayout renders all included entities in correct section order with appropriate formatting
- DOCX export includes all non-empty entity sections
- Submission snapshots capture all entity inclusion/exclusion state
- Old snapshots (without new entity data) still render correctly
</success_criteria>

<output>
After completion, create `.planning/phases/06-projects-in-export-pipeline-and-resume-json-import/06-04-SUMMARY.md`
</output>
