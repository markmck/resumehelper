---
phase: 06-projects-in-export-pipeline-and-resume-json-import
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - src/renderer/src/components/ExperienceTab.tsx
  - src/renderer/src/components/EducationList.tsx
  - src/renderer/src/components/VolunteerList.tsx
  - src/renderer/src/components/AwardList.tsx
  - src/renderer/src/components/PublicationList.tsx
  - src/renderer/src/components/LanguageList.tsx
  - src/renderer/src/components/InterestList.tsx
  - src/renderer/src/components/ReferenceList.tsx
autonomous: true
requirements:
  - IMP-01

must_haves:
  truths:
    - "All 7 new entity sections appear in Experience tab with add/edit/delete capabilities"
    - "All sections in Experience tab (Profile, Work History, Skills, Projects, and all new sections) are collapsible"
    - "Primary sections (Work History, Skills, Projects, Education, Volunteer) default expanded; minor sections (Awards, Publications, Languages, Interests, References) default collapsed"
  artifacts:
    - path: "src/renderer/src/components/ExperienceTab.tsx"
      provides: "Collapsible wrappers for all sections + import button placeholder"
      contains: "EducationList"
    - path: "src/renderer/src/components/EducationList.tsx"
      provides: "Education CRUD UI with inline editing"
      exports: ["default"]
    - path: "src/renderer/src/components/VolunteerList.tsx"
      provides: "Volunteer CRUD UI"
      exports: ["default"]
  key_links:
    - from: "src/renderer/src/components/ExperienceTab.tsx"
      to: "src/renderer/src/components/EducationList.tsx"
      via: "import and render inside collapsible section"
      pattern: "EducationList"
    - from: "src/renderer/src/components/EducationList.tsx"
      to: "window.api.education"
      via: "IPC calls for CRUD"
      pattern: "window.api.education"
---

<objective>
Create Experience tab UI components for all 7 new resume.json entities with collapsible sections and CRUD functionality.

Purpose: Users need to add/edit/delete education, volunteer, awards, publications, languages, interests, and references from the Experience tab. The collapsible layout keeps the tab organized as the number of sections grows.

Output: 7 new list components rendered in a collapsible-section ExperienceTab. All existing sections (Profile, Work History, Skills, Projects) gain collapse/expand behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-projects-in-export-pipeline-and-resume-json-import/06-RESEARCH.md
@.planning/phases/06-projects-in-export-pipeline-and-resume-json-import/06-02-SUMMARY.md

@src/renderer/src/components/ExperienceTab.tsx
@src/renderer/src/components/ProjectList.tsx
@src/renderer/src/components/JobList.tsx
@src/preload/index.d.ts

<interfaces>
<!-- Entity types created in Plan 02 (read from 06-02-SUMMARY.md or index.d.ts) -->
<!-- Executor: read src/preload/index.d.ts to get exact Education, Volunteer, Award, etc. interfaces -->

From ExperienceTab.tsx (existing collapsible pattern — Profile section):
```typescript
const [profileOpen, setProfileOpen] = useState(false)
<button onClick={() => setProfileOpen(prev => !prev)}
  className="flex items-center gap-2 text-sm font-semibold text-zinc-400 uppercase tracking-wider hover:text-zinc-300 transition-colors"
  style={{ marginBottom: profileOpen ? '16px' : '0' }}>
  <span style={{ display: 'inline-block', transform: profileOpen ? 'rotate(90deg)' : 'rotate(0deg)', transition: 'transform 0.15s' }}>&#9654;</span>
  Profile
</button>
{profileOpen && <ProfileSettings />}
```

From ProjectList.tsx (existing list component pattern — use as template for new components):
<!-- Executor: read src/renderer/src/components/ProjectList.tsx for the add form + list + inline edit pattern -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 7 new entity list components following ProjectList/JobList pattern</name>
  <files>src/renderer/src/components/EducationList.tsx, src/renderer/src/components/VolunteerList.tsx, src/renderer/src/components/AwardList.tsx, src/renderer/src/components/PublicationList.tsx, src/renderer/src/components/LanguageList.tsx, src/renderer/src/components/InterestList.tsx, src/renderer/src/components/ReferenceList.tsx</files>
  <action>
Create 7 new React components, each following the same pattern as ProjectList.tsx (read it first for the exact pattern). Each component:
- Uses `useState` to hold the entity list, loaded via `useEffect` calling `window.api.{entity}.list()`
- Has an add form at the top with relevant fields
- Renders a list of items below with inline editing via InlineEdit component (import from './InlineEdit')
- Each item has a delete button (trash icon or X)
- Uses the same dark theme styling (zinc colors, etc.) as existing components
- Uses inline styles for spacing (not Tailwind margin/padding utilities — per project convention)

Entity-specific field details:

**EducationList.tsx**: Fields: institution (required), area, studyType, startDate, endDate, score. Courses displayed as comma-separated tags (read-only display of JSON array, editable as comma-separated text). Add form: institution + area inputs. Item display: "Institution — Area (StudyType)" with dates. Courses shown below as small tag chips.

**VolunteerList.tsx**: Fields: organization (required), position, startDate, endDate, summary. Highlights displayed as bullet list (read-only display of JSON array). Add form: organization + position inputs. Item display: "Organization — Position" with dates.

**AwardList.tsx**: Fields: title (required), date, awarder, summary. Add form: title input. Item display: "Title — Awarder (Date)". Summary shown below in smaller text.

**PublicationList.tsx**: Fields: name (required), publisher, releaseDate, url, summary. Add form: name input. Item display: "Name — Publisher (Date)". URL and summary below.

**LanguageList.tsx**: Fields: language (required), fluency. Simplest component. Add form: language + fluency inputs side by side. Item display: "Language — Fluency" in a compact row.

**InterestList.tsx**: Fields: name (required), keywords (JSON array). Add form: name input. Item display: "Name" with keywords as tag chips below. Keywords editable as comma-separated text.

**ReferenceList.tsx**: Fields: name (required), reference (the text). Add form: name input. Item display: name as header, reference text below.

For all components: After create/update/delete, re-fetch the list from the API (same pattern as ProjectList). Use `useToast` from Toast.tsx for error feedback if needed.

NOTE: These are standalone CRUD components. They do NOT need to be complex — keep them simple like ProjectList. The inline editing pattern from JobItem/ProjectItem should be adapted: for entities with few fields, inline editing can use simple text inputs that save on blur.
  </action>
  <verify>
    <automated>cd D:/Projects/resumeHelper && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>7 new list components exist, each with add form, item list with inline editing, and delete functionality. All use window.api.{entity} for CRUD. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ExperienceTab to use collapsible sections for ALL sections</name>
  <files>src/renderer/src/components/ExperienceTab.tsx</files>
  <action>
Refactor ExperienceTab.tsx to make all sections collapsible:

1. Import all 7 new list components (EducationList, VolunteerList, AwardList, PublicationList, LanguageList, InterestList, ReferenceList).

2. Replace the current hardcoded section rendering with a collapsible pattern for EVERY section. Extract the collapsible section pattern into a local helper component or inline it for each section. The pattern (already used for Profile):
   ```tsx
   const [sectionOpen, setSectionOpen] = useState(defaultExpanded)
   <button onClick={() => setSectionOpen(prev => !prev)} ...>
     <span style={{ transform: sectionOpen ? 'rotate(90deg)' : 'rotate(0deg)' }}>&#9654;</span>
     Section Name
   </button>
   {sectionOpen && <SectionComponent />}
   ```

3. Use individual useState hooks for each section (or a single object state — Claude's discretion). Default expanded/collapsed per user decision:
   - Profile: collapsed (existing behavior)
   - Work History: expanded (new — was always visible before)
   - Skills: expanded
   - Projects: expanded
   - Education: expanded
   - Volunteer: expanded
   - Awards: collapsed
   - Publications: collapsed
   - Languages: collapsed
   - Interests: collapsed
   - References: collapsed

4. Section order in the tab:
   Profile, Work History, Skills, Projects, Education, Volunteer, Awards, Publications, Languages, Interests, References

5. Each section wrapped in `<section style={{ marginBottom: '48px' }}>` (existing pattern). The section heading button uses the same className as the Profile section: `"flex items-center gap-2 text-sm font-semibold text-zinc-400 uppercase tracking-wider hover:text-zinc-300 transition-colors"`.

6. Do NOT add the Import button in this task — that comes in Plan 05.
  </action>
  <verify>
    <automated>cd D:/Projects/resumeHelper && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>ExperienceTab renders all 11 sections (Profile + 3 existing + 7 new) with collapsible expand/collapse toggles. Primary sections default expanded, minor sections default collapsed. Section order matches spec. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npm run build` succeeds
3. Manual smoke test: Open Experience tab, verify all sections appear with collapse toggles. Primary sections (Work History, Skills, Projects, Education, Volunteer) should be expanded. Minor sections (Awards, Publications, Languages, Interests, References) should be collapsed. Click a section header to toggle. Add an education entry — verify it persists after page navigation. Delete it — verify removal.
</verification>

<success_criteria>
- All 7 new entity sections visible in Experience tab with working CRUD
- All 11 sections have collapsible headers with arrow indicators
- Default expanded/collapsed states match the specification
- Existing sections (Profile, Work History, Skills, Projects) continue working with their current UI, just wrapped in collapsible headers
</success_criteria>

<output>
After completion, create `.planning/phases/06-projects-in-export-pipeline-and-resume-json-import/06-03-SUMMARY.md`
</output>
