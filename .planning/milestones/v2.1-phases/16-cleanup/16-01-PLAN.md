---
phase: 16-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/main/handlers/themes.ts
  - src/main/handlers/index.ts
  - src/main/lib/themeRegistry.ts
  - src/main/handlers/export.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
  - src/renderer/src/components/VariantEditor.tsx
autonomous: true
requirements: [CLEAN-01]

must_haves:
  truths:
    - "No theme npm packages remain in package.json (even, class, elegant)"
    - "No IPC handler for themes:list, themes:renderHtml, or themes:renderSnapshotHtml exists"
    - "window.api.themes is completely gone from preload bridge and type declarations"
    - "VariantEditor has no themes state, no themes useEffect, no themes dropdown JSX"
    - "export:pdf handler has no dead theme branch — only the print.html path remains"
    - "buildResumeJson still works (ai.ts import unbroken)"
    - "App builds without TypeScript errors"
  artifacts:
    - path: "src/main/handlers/themes.ts"
      provides: "DELETED — file must not exist"
    - path: "src/main/lib/themeRegistry.ts"
      provides: "buildResumeJson only — renderThemeHtml and sanitizeDates removed"
    - path: "src/main/handlers/export.ts"
      provides: "export:pdf with only print.html path, no dead theme branch"
  key_links:
    - from: "src/main/handlers/ai.ts"
      to: "src/main/lib/themeRegistry.ts"
      via: "import { buildResumeJson }"
      pattern: "import.*buildResumeJson.*themeRegistry"
---

<objective>
Remove all old resume.json theme infrastructure: uninstall 3 npm packages, delete the themes.ts handler file, clean themeRegistry.ts (keep only buildResumeJson), remove themes preload bridge, strip VariantEditor of old themes state/dropdown, and remove the dead theme branch from export:pdf.

Purpose: Eliminate dead code paths left over from v1.0 theme system, now fully replaced by the v2.1 HTML/CSS template pipeline.
Output: Clean codebase with no theme package imports, no theme IPC handlers, and a simplified export:pdf path.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cleanup/16-CONTEXT.md
@.planning/phases/16-cleanup/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Uninstall theme packages and delete themes.ts handler</name>
  <files>package.json, package-lock.json, src/main/handlers/themes.ts, src/main/handlers/index.ts, src/main/lib/themeRegistry.ts</files>
  <action>
1. Run `npm uninstall jsonresume-theme-even @jsonresume/jsonresume-theme-class jsonresume-theme-elegant` to remove the 3 old theme packages.

2. Delete `src/main/handlers/themes.ts` entirely — this file contains registerThemeHandlers with themes:list, themes:renderHtml, themes:renderSnapshotHtml IPC handlers. All three are dead after this phase.

3. In `src/main/handlers/index.ts`:
   - Remove the import of registerThemeHandlers from './themes'
   - Remove the registerThemeHandlers(db) call (or equivalent invocation)
   - Keep all other handler registrations intact

4. In `src/main/lib/themeRegistry.ts`:
   - Delete the `renderThemeHtml()` function entirely
   - Delete the `sanitizeDates()` helper function entirely
   - Delete the `THEMES` array and `THEME_KEYS` constant IF grep confirms they are only imported by themes.ts (which is now deleted). If any other file imports them, keep them.
   - Delete the `ThemeEntry` type IF `THEMES` is deleted (same grep check)
   - **KEEP `buildResumeJson()`** — it is imported by `src/main/handlers/ai.ts` and must remain functional
   - Remove any imports that are now unused (e.g., the jsonresume-theme-* package imports)
   - Clean up the file so only buildResumeJson and its dependencies remain

5. Grep the entire codebase for any remaining references to the deleted functions/types/constants to confirm no broken imports:
   - `renderThemeHtml`
   - `sanitizeDates`
   - `THEMES` (the array, not the word "themes" generically)
   - `THEME_KEYS`
   - `ThemeEntry`
   - `registerThemeHandlers`
  </action>
  <verify>
    <automated>cd D:/Projects/resumeHelper && npm run build</automated>
  </verify>
  <done>Three theme npm packages uninstalled. themes.ts handler file deleted. themeRegistry.ts contains only buildResumeJson. index.ts no longer registers theme handlers. Build passes with no TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Remove themes preload bridge, VariantEditor themes code, and export:pdf dead branch</name>
  <files>src/preload/index.ts, src/preload/index.d.ts, src/renderer/src/components/VariantEditor.tsx, src/main/handlers/export.ts</files>
  <action>
1. In `src/preload/index.ts`:
   - Remove the entire `themes: { ... }` block from the api object (the block that exposes themes.list, themes.renderHtml, themes.renderSnapshotHtml to the renderer)
   - Remove any associated IPC invoke calls for themes channels

2. In `src/preload/index.d.ts`:
   - Remove the `themes: { ... }` type declaration block from the Api interface
   - This ensures TypeScript catches any remaining renderer code that tries to use window.api.themes

3. In `src/renderer/src/components/VariantEditor.tsx`:
   - Remove the `themes` state declaration (useState for themes array, around line 59)
   - Remove the `useEffect` that calls `window.api.themes.list().then(setThemes)` (around lines 84-86)
   - Remove the themes dropdown JSX block (around lines 424-447 — the conditional rendering of the old theme selector)
   - If `handleThemeChange` is ONLY called from the removed themes dropdown, remove it too. If it's also wired to the preview header template selector (Phase 15 CTRL-01), keep it.
   - Do NOT remove `layoutTemplate` state — it is still used by the preview header template dropdown
   - Remove any imports that become unused after these deletions

4. In `src/main/handlers/export.ts`:
   - Remove the `import { renderThemeHtml } from '../lib/themeRegistry'` import (or similar — may also import buildResumeJson; keep that import if so)
   - In the `export:pdf` handler, remove the dead `else` branch (approximately lines 296-344) that handled old theme rendering via renderThemeHtml. The V2_TEMPLATES Set now covers all 5 valid template keys, so this branch is unreachable. Simplify so only the print.html BrowserWindow path remains.
   - Do NOT modify the `export:snapshotPdf` handler yet — that is Plan 02's scope. It will still reference renderThemeHtml which is now deleted, but since we'll rewrite it entirely in Plan 02, a temporary build error here is acceptable OR wrap the snapshotPdf handler body in a `// TODO: rewrite in Plan 02` comment with a stub that returns `{ canceled: true, error: 'snapshot export being migrated' }`.

5. Grep the renderer codebase for any remaining `window.api.themes` references to confirm complete removal. Check SnapshotViewer.tsx — it may have a themes reference that will be cleaned in Plan 02 when SnapshotViewer is rewritten.
  </action>
  <verify>
    <automated>cd D:/Projects/resumeHelper && npm run build</automated>
  </verify>
  <done>Preload bridge has no themes namespace. VariantEditor has no themes state, effect, or dropdown. export:pdf uses only the print.html path with no dead theme branch. Build passes cleanly. The only remaining old-system reference is the snapshotPdf handler (addressed in Plan 02).</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero TypeScript errors
2. `grep -r "jsonresume-theme" package.json` returns nothing
3. `grep -r "renderThemeHtml" src/` returns nothing (except possibly a TODO comment in snapshotPdf)
4. `grep -r "window.api.themes" src/renderer/` returns nothing (except possibly SnapshotViewer which is rewritten in Plan 02)
5. `grep -r "registerThemeHandlers" src/` returns nothing
6. The file `src/main/handlers/themes.ts` does not exist
</verification>

<success_criteria>
All 3 old theme npm packages are uninstalled. The themes.ts handler file is deleted. themeRegistry.ts retains only buildResumeJson (ai.ts import works). The preload themes bridge is removed from both implementation and type declarations. VariantEditor has no theme-related state or UI. The export:pdf handler has a single clean code path through print.html. The app builds without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/16-cleanup/16-01-SUMMARY.md`
</output>
